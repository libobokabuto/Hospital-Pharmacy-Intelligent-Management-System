# 🚀 项目启动指南

## 快速开始

按照以下步骤，3分钟内启动项目！

---

## 📋 前置条件

确保已完成环境搭建：
- ✅ JDK 17+
- ✅ MySQL 8.0
- ✅ Maven 3.6+
- ✅ Python 3.8+
- ✅ Node.js 16+
- ✅ 数据库已初始化

---

## 🎯 快速启动 (推荐)

### Windows用户
1. 进入项目根目录
2. 双击运行 `start.bat` 文件
3. 等待所有服务启动完成
4. 打开浏览器访问 http://localhost:5173

### Linux/Mac用户
```bash
cd Hospital-Pharmacy-Intelligent-Management-System
chmod +x start.sh
./start.sh
```

---

## 🔧 手动启动步骤

如果自动脚本有问题，按以下步骤手动启动：

### 第1步：启动后端服务

```bash
# 进入后端目录
cd backend

# 安装依赖
mvn clean install

# 启动Spring Boot应用
mvn spring-boot:run
```

**成功标志**: 控制台显示 "Started HpimsApplication" 和端口8080

### 第2步：启动前端服务

```bash
# 进入前端目录
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

**成功标志**: 控制台显示 "Local: http://localhost:5173"

### 第3步：启动Python审计服务

```bash
# 进入Python服务目录
cd python-audit-service

# 安装依赖
pip install -r requirements.txt

# 启动服务
python src/app.py
```

**成功标志**: 控制台显示 "Running on http://localhost:5000"

---

## 🌐 访问系统

### 主入口
- **前端界面**: http://localhost:5173
- **后端API**: http://localhost:8080
- **Python服务**: http://localhost:5000

### 默认账号
- **用户名**: admin
- **密码**: 123456

---

## 📊 服务状态检查

### 检查后端服务
打开浏览器访问：http://localhost:8080/api/test/hello

**期望响应**:
```json
{
  "message": "Hello, HPIMS!",
  "timestamp": "2024-12-09T12:00:00"
}
```

### 检查数据库连接
访问：http://localhost:8080/api/users

**期望响应**: 返回用户列表

### 检查Python服务
访问：http://localhost:5000/health

**期望响应**:
```json
{
  "status": "healthy",
  "service": "python-audit-service"
}
```

---

## 🛠 开发模式

### 热重载
- 前端修改代码后自动刷新
- 后端修改需要重启服务

### 调试模式
```bash
# 后端调试
mvn spring-boot:run -Dspring-boot.run.jvmArguments="-Xdebug -Xrunjdwp:transport=dt_socket,server=y,suspend=n,address=5005"

# 前端调试
npm run dev -- --host 0.0.0.0
```

---

## 🔄 服务管理

### 停止服务
- 按 `Ctrl+C` 停止当前服务
- 或关闭命令行窗口

### 重启服务
```bash
# 后端重启
cd backend && mvn spring-boot:run

# 前端重启
cd frontend && npm run dev

# Python服务重启
cd python-audit-service && python src/app.py
```

---

## 🚨 故障排除

### 端口冲突
```bash
# 检查端口占用
netstat -ano | findstr :8080
netstat -ano | findstr :5173
netstat -ano | findstr :5000

# 杀死进程 (Windows)
taskkill /PID <进程ID> /F
```

### 依赖问题
```bash
# 清理缓存
cd frontend && rm -rf node_modules && npm install
cd backend && mvn clean install -U
```

### 数据库连接失败
1. 检查MySQL服务是否启动
2. 验证用户名密码
3. 检查防火墙设置

---

## 📈 性能优化

### 生产环境配置
```bash
# 后端生产模式
mvn spring-boot:run -Dspring.profiles.active=prod

# 前端构建
cd frontend && npm run build
```

### 内存配置
```bash
# 增加JVM内存
mvn spring-boot:run -Dspring-boot.run.jvmArguments="-Xmx2g -Xms1g"
```

---

## 📝 日志查看

### 后端日志
```
backend/logs/spring.log
```

### 前端日志
浏览器开发者工具的Console面板

### Python日志
```
python-audit-service/logs/audit.log
```

---

## 🎉 成功启动标志

当你看到以下信息时，说明项目启动成功：

1. **后端**: `Started HpimsApplication in X seconds`
2. **前端**: `Local: http://localhost:5173`
3. **Python**: `Running on http://localhost:5000`
4. **浏览器**: 成功加载登录页面

---

## 📞 寻求帮助

如果遇到问题：
1. 检查控制台错误信息
2. 验证环境配置
3. 查看常见问题文档
4. 联系开发团队

祝你顺利启动项目！🎊

