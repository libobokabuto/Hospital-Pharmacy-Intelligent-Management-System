启动 MySQL 数据库
确保 MySQL 8.0 正在运行。
初始化数据库
# 登录MySQLmysql -u root -p# 创建数据库CREATE DATABASE hpims CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;# 执行初始化脚本USE hpims;SOURCE database/init.sql;
配置数据库连接
编辑 backend/src/main/resources/application.yml，修改数据库密码：
spring:  datasource:    password: 你的MySQL密码  # 默认是 20050129
启动后端服务
cd backendmvn clean installmvn spring-boot:run
等待看到 "Started HpimsApplication" 表示启动成功，端口 8080。
启动 Python 审核服务
打开新的终端窗口：
cd python-audit-servicepip install -r requirements.txtpython src/app.py
等待看到 "Running on http://localhost:5000" 表示启动成功。
启动前端应用
再打开一个新的终端窗口：
cd frontendnpm installnpm run dev
等待看到 "Local: http://localhost:3000" 表示启动成功。
访问系统
打开浏览器访问：http://localhost:3000
使用默认账号登录：
管理员：admin / 123456
医生：doctor / 123456
药师：pharmacist / 123456
快速检查：
后端：http://localhost:8080/api/swagger-ui.html（能看到API文档说明后端正常）
Python服务：http://localhost:5000/health（返回 {"status": "healthy"} 说明正常）
如果遇到问题：
端口被占用：修改对应配置文件中的端口号
数据库连接失败：检查MySQL是否运行，密码是否正确
Python服务无法连接：检查是否安装了所有依赖（pip install -r requirements.txt）
按以上步骤即可运行。