# 3 总体设计

## 3.1 系统架构设计

本系统采用前后端分离的三层架构设计，包括表示层（前端）、应用服务层（后端）和数据层（数据库）。系统整体架构清晰，各层职责分明，便于开发和维护。

### 3.1.1 架构方案论证

#### 3.1.1.1 体系结构风格选择

本系统采用**前后端分离架构**（B/S架构），主要基于以下考虑：

**（1）前后端分离架构的优势：**
- **开发效率高**：前后端可以并行开发，减少相互依赖，提高开发效率
- **技术选型灵活**：前端和后端可以使用不同的技术栈，选择最适合的技术
- **维护成本低**：前后端代码分离，职责清晰，便于维护和升级
- **性能优化**：前端可以使用CDN加速，后端可以独立进行性能优化
- **扩展性强**：可以轻松支持多端（Web、移动端）访问，只需开发不同的前端应用

**（2）选择Spring Boot作为后端框架的理由：**
- **快速开发**：Spring Boot提供了大量的自动配置，减少了繁琐的配置工作
- **生态成熟**：Spring生态系统成熟，有丰富的第三方库支持
- **微服务支持**：Spring Boot天然支持微服务架构，便于后续扩展
- **JPA支持**：Spring Data JPA简化了数据访问层的开发
- **安全框架**：Spring Security提供了完善的安全认证和授权机制

**（3）选择Vue3作为前端框架的理由：**
- **轻量高效**：Vue3相比Vue2性能提升显著，体积更小
- **组件化开发**：支持组件化开发，代码复用率高
- **生态丰富**：Element Plus等UI组件库提供了丰富的组件
- **学习成本低**：Vue语法简洁，易于上手
- **响应式系统**：Vue3的响应式系统更加高效

**（4）选择Python Flask作为审核服务框架的理由：**
- **算法支持好**：Python有丰富的科学计算和机器学习库（pandas、numpy、scikit-learn）
- **开发快速**：Flask轻量级，开发效率高
- **易于集成**：通过RESTful API可以轻松与Java后端集成
- **灵活性强**：Python适合快速开发和原型验证

#### 3.1.1.2 分层架构设计

系统采用经典的三层架构模式：

**（1）表示层（Presentation Layer）**
- 职责：用户界面展示和用户交互
- 技术：Vue3 + Element Plus + Pinia
- 特点：无状态设计，所有业务逻辑在服务端处理

**（2）应用服务层（Application Service Layer）**
- 职责：业务逻辑处理、数据验证、权限控制
- 技术：Spring Boot + Spring Security + JPA
- 特点：RESTful API设计，无状态服务

**（3）数据层（Data Layer）**
- 职责：数据存储和管理
- 技术：MySQL 8.0
- 特点：关系型数据库，保证数据一致性和完整性

### 3.1.2 硬件拓扑设计

系统硬件拓扑结构如下：

**（1）客户端设备**
- 用户通过PC或移动设备的浏览器访问系统
- 支持Windows、macOS、Linux等操作系统
- 支持主流浏览器（Chrome、Firefox、Edge、Safari等）

**（2）Web服务器**
- 部署前端静态资源（Vue3构建后的静态文件）
- 可通过Nginx或直接部署在Spring Boot内嵌Tomcat服务器
- 推荐配置：2核CPU，4GB内存

**（3）应用服务器**
- 部署Spring Boot后端服务
- 处理业务逻辑和API请求
- 推荐配置：4核CPU，8GB内存

**（4）Python审核服务服务器**
- 部署Flask审核服务
- 独立运行，提供审核算法服务
- 推荐配置：2核CPU，4GB内存

**（5）数据库服务器**
- 部署MySQL数据库
- 存储系统所有业务数据
- 推荐配置：4核CPU，8GB内存，SSD硬盘

**硬件部署说明：**
- 客户端通过HTTP/HTTPS协议访问Web服务器
- Web服务器和应用服务器可以部署在同一台服务器上（开发环境），也可以分离部署（生产环境）
- 应用服务器通过内网访问数据库服务器
- 应用服务器通过HTTP协议调用Python审核服务

**硬件拓扑图元素说明：**
（注：用户可自行绘制硬件拓扑图，以下为图的元素描述）

```
图形元素：
- 客户端设备（PC/移动设备）: 矩形框，标注"客户端浏览器"
- Web服务器: 矩形框，标注"Web服务器 (Nginx/静态资源)"
- 应用服务器: 矩形框，标注"应用服务器 (Spring Boot)"
- Python服务: 矩形框，标注"Python审核服务 (Flask)"
- 数据库服务器: 圆柱体，标注"MySQL数据库"

连接关系：
- 客户端 ←→ Web服务器 (HTTP/HTTPS，双向箭头)
- Web服务器 ←→ 应用服务器 (HTTP，双向箭头)
- 应用服务器 ←→ 数据库服务器 (JDBC，双向箭头)
- 应用服务器 ←→ Python服务 (HTTP RESTful API，双向箭头)

网络标注：
- 客户端到Web服务器：公网/局域网
- Web服务器到应用服务器：内网
- 应用服务器到数据库服务器：内网
- 应用服务器到Python服务：内网/本地回环
```

### 3.1.3 软件架构设计

系统软件架构采用分层模块化设计，主要分为以下层次：

**（1）前端架构层**
- **路由层**：Vue Router管理页面路由和导航
- **状态管理层**：Pinia管理全局状态（用户信息、登录状态等）
- **视图层**：Vue组件展示用户界面
- **API层**：Axios封装HTTP请求，与后端API通信

**（2）后端架构层**
- **控制器层（Controller）**：接收HTTP请求，处理请求参数，调用Service层，返回响应
- **业务逻辑层（Service）**：实现业务逻辑，事务管理，调用Repository层
- **数据访问层（Repository）**：数据持久化操作，使用JPA进行ORM映射
- **实体层（Model）**：数据模型定义，对应数据库表结构

**（3）安全架构层**
- **认证过滤器**：JWT认证过滤器，验证用户身份
- **权限控制**：Spring Security实现基于角色的访问控制（RBAC）
- **密码加密**：BCrypt加密算法保护用户密码

**（4）外部服务层**
- **Python审核服务**：独立的审核算法服务，通过RESTful API调用

**软件架构图元素说明：**
（注：用户可自行绘制软件架构图，以下为图的元素描述）

```
图形元素（从上到下）：
1. 前端层：
   - 矩形框："视图组件 (Vue Components)"
   - 矩形框："路由管理 (Vue Router)"
   - 矩形框："状态管理 (Pinia)"
   - 矩形框："HTTP客户端 (Axios)"

2. 网关/接口层：
   - 矩形框："HTTP/HTTPS接口"

3. 后端层：
   - 矩形框："控制器层 (Controller)"
     - AuthController
     - UserController
     - MedicineController
     - PrescriptionController
     - StockController
     - AuditController
   - 矩形框："业务逻辑层 (Service)"
     - UserService
     - MedicineService
     - PrescriptionService
     - StockInService / StockOutService
     - AuditRecordService
     - AuditServiceClient
   - 矩形框："数据访问层 (Repository)"
     - UserRepository
     - MedicineRepository
     - PrescriptionRepository
     - ...
   - 矩形框："实体层 (Model)"
     - User, Medicine, Prescription, ...

4. 安全层：
   - 矩形框："JWT认证过滤器"
   - 矩形框："Spring Security"

5. 外部服务：
   - 矩形框："Python审核服务 (Flask)"

6. 数据层：
   - 圆柱体："MySQL数据库"

数据流向（箭头）：
- 前端 → 控制器层（HTTP请求，带数据）
- 控制器层 → 业务逻辑层（方法调用，带参数）
- 业务逻辑层 → 数据访问层（方法调用）
- 数据访问层 → 数据库（SQL查询，带数据）
- 业务逻辑层 → Python服务（HTTP请求，带数据）
- 数据库 → 数据访问层（查询结果）
- Python服务 → 业务逻辑层（HTTP响应，带数据）
- 各层依次向上返回结果

调用关系标注：
- 使用实线箭头表示调用关系
- 箭头方向表示调用方向
- 可以标注HTTP、JDBC、RESTful API等协议
```

### 3.1.4 系统功能划分

系统按照功能模块进行划分，主要分为以下几个模块：

#### 3.1.4.1 用户认证与权限管理模块

**模块标识符**：M-AUTH

**模块功能**：
- 用户登录认证
- 用户注册（管理员创建用户）
- JWT令牌生成和验证
- 基于角色的权限控制（RBAC）
- 用户信息管理

**模块组成**：
- AuthController：认证相关API接口
- UserController：用户管理API接口
- UserService：用户业务逻辑
- JwtUtil：JWT工具类
- SecurityConfig：Spring Security配置

#### 3.1.4.2 药品管理模块

**模块标识符**：M-MED

**模块功能**：
- 药品信息的增删改查（CRUD）
- 药品搜索和分类查询
- 低库存预警
- 药品信息维护

**模块组成**：
- MedicineController：药品管理API接口
- MedicineService：药品业务逻辑
- MedicineRepository：药品数据访问
- Medicine实体类：药品数据模型

#### 3.1.4.3 库存管理模块

**模块标识符**：M-STOCK

**模块功能**：
- 药品入库管理
- 药品出库管理
- 库存查询和统计
- 库存自动更新
- 库存预警

**模块组成**：
- StockController：库存管理API接口
- StockInService：入库业务逻辑
- StockOutService：出库业务逻辑
- StockInRepository / StockOutRepository：库存记录数据访问
- StockIn / StockOut实体类：库存记录数据模型

#### 3.1.4.4 处方管理模块

**模块标识符**：M-PRES

**模块功能**：
- 处方录入
- 处方查询和详情查看
- 处方修改和取消
- 处方状态管理
- 处方明细管理

**模块组成**：
- PrescriptionController：处方管理API接口
- PrescriptionService：处方业务逻辑
- PrescriptionDetailService：处方明细业务逻辑
- PrescriptionRepository / PrescriptionDetailRepository：处方数据访问
- Prescription / PrescriptionDetail实体类：处方数据模型

#### 3.1.4.5 审核管理模块

**模块标识符**：M-AUDIT

**模块功能**：
- 处方自动审核（调用Python服务）
- 处方人工审核
- 审核记录查询和统计
- 审核历史追踪

**模块组成**：
- PrescriptionController（部分）：审核相关API接口
- AuditController：审核记录查询API接口
- PrescriptionService（部分）：自动审核和人工审核业务逻辑
- AuditRecordService：审核记录业务逻辑
- AuditServiceClient：Python审核服务客户端
- AuditRecordRepository：审核记录数据访问
- AuditRecord实体类：审核记录数据模型

#### 3.1.4.6 系统模块控制关系

**控制关系说明**：

```
用户认证与权限管理模块 (M-AUTH)
    ↓ (控制)
    ├─ 药品管理模块 (M-MED) - 需要登录认证和权限验证
    ├─ 库存管理模块 (M-STOCK) - 需要登录认证和权限验证
    ├─ 处方管理模块 (M-PRES) - 需要登录认证和权限验证
    └─ 审核管理模块 (M-AUDIT) - 需要登录认证和权限验证

处方管理模块 (M-PRES)
    ↓ (调用)
    ├─ 药品管理模块 (M-MED) - 查询药品信息
    ├─ 库存管理模块 (M-STOCK) - 发药时扣减库存
    └─ 审核管理模块 (M-AUDIT) - 提交审核

审核管理模块 (M-AUDIT)
    ↓ (调用)
    ├─ 药品管理模块 (M-MED) - 获取药品适应症、禁忌症等信息
    └─ Python审核服务 (外部服务) - 调用审核算法

库存管理模块 (M-STOCK)
    ↓ (更新)
    └─ 药品管理模块 (M-MED) - 更新药品库存数量
```

**模块层次关系**：

- **上层模块**：用户认证与权限管理模块（为其他模块提供基础服务）
- **业务模块**：药品管理模块、库存管理模块、处方管理模块、审核管理模块（核心业务功能）
- **外部服务**：Python审核服务（独立的审核算法服务）

### 3.1.5 功能需求与系统模块的关系

本小节通过矩阵图说明各项功能需求的实现与各模块的分配关系。其中，"●"表示主要实现模块，"○"表示辅助实现模块。

#### 表3.1 用户管理功能需求与模块关系表

| 功能需求 | M-AUTH<br/>用户认证与权限管理 | M-MED<br/>药品管理 | M-STOCK<br/>库存管理 | M-PRES<br/>处方管理 | M-AUDIT<br/>审核管理 |
|---------|:---:|:---:|:---:|:---:|:---:|
| 用户登录 | ● |  |  |  |  |
| 用户注册 | ● |  |  |  |  |
| 用户信息查询 | ● |  |  |  |  |
| 用户信息创建 | ● |  |  |  |  |
| 用户信息修改 | ● |  |  |  |  |
| 用户信息删除 | ● |  |  |  |
| 角色权限管理 | ● |  |  |  |  |

#### 表3.2 药品管理功能需求与模块关系表

| 功能需求 | M-AUTH<br/>用户认证与权限管理 | M-MED<br/>药品管理 | M-STOCK<br/>库存管理 | M-PRES<br/>处方管理 | M-AUDIT<br/>审核管理 |
|---------|:---:|:---:|:---:|:---:|:---:|
| 药品信息查询 | ○ | ● |  |  |  |
| 药品信息创建 | ○ | ● |  |  |  |
| 药品信息修改 | ○ | ● |  |  |  |
| 药品信息删除 | ○ | ● |  |  |  |
| 药品搜索 | ○ | ● |  |  |  |
| 按分类查询药品 | ○ | ● |  |  |  |
| 低库存预警 | ○ | ● | ○ |  |  |

#### 表3.3 库存管理功能需求与模块关系表

| 功能需求 | M-AUTH<br/>用户认证与权限管理 | M-MED<br/>药品管理 | M-STOCK<br/>库存管理 | M-PRES<br/>处方管理 | M-AUDIT<br/>审核管理 |
|---------|:---:|:---:|:---:|:---:|:---:|
| 库存查询 | ○ | ● | ● |  |  |
| 药品入库 | ○ | ● | ● |  |  |
| 药品出库 | ○ | ● | ● | ○ |  |
| 入库记录查询 | ○ |  | ● |  |  |
| 出库记录查询 | ○ |  | ● |  |  |
| 库存统计 | ○ |  | ● |  |  |
| 库存自动更新 |  | ● | ● | ○ |  |

#### 表3.4 处方管理功能需求与模块关系表

| 功能需求 | M-AUTH<br/>用户认证与权限管理 | M-MED<br/>药品管理 | M-STOCK<br/>库存管理 | M-PRES<br/>处方管理 | M-AUDIT<br/>审核管理 |
|---------|:---:|:---:|:---:|:---:|:---:|
| 处方录入 | ○ | ○ |  | ● |  |
| 处方查询 | ○ |  |  | ● |  |
| 处方详情查看 | ○ | ○ |  | ● |  |
| 处方修改 | ○ |  |  | ● |  |
| 处方取消 | ○ |  |  | ● |  |
| 处方明细管理 | ○ | ○ |  | ● |  |
| 处方发药 | ○ |  | ● | ● |  |

#### 表3.5 审核管理功能需求与模块关系表

| 功能需求 | M-AUTH<br/>用户认证与权限管理 | M-MED<br/>药品管理 | M-STOCK<br/>库存管理 | M-PRES<br/>处方管理 | M-AUDIT<br/>审核管理 |
|---------|:---:|:---:|:---:|:---:|:---:|
| 提交审核 | ○ |  |  | ● | ● |
| 自动审核 |  | ● |  | ○ | ● |
| 人工审核 | ○ |  |  | ○ | ● |
| 审核记录查询 | ○ |  |  |  | ● |
| 审核历史查询 | ○ |  |  | ○ | ● |
| 审核统计 | ○ |  |  |  | ● |

#### 功能需求与模块关系说明

**（1）模块依赖关系：**
- M-AUTH模块是所有业务模块的基础，提供用户认证和权限验证服务
- M-PRES模块依赖M-MED模块（查询药品信息）和M-STOCK模块（发药时扣减库存）
- M-AUDIT模块依赖M-MED模块（获取药品适应症、禁忌症等信息）和M-PRES模块（获取处方信息）

**（2）模块协作关系：**
- 处方发药功能需要M-PRES模块和M-STOCK模块协作完成
- 自动审核功能需要M-AUDIT模块调用Python服务，并依赖M-MED模块和M-PRES模块
- 库存管理需要与M-MED模块协作，实现库存数量的自动更新

## 3.2 系统接口设计

系统接口设计包括用户接口、外部接口和内部接口三个部分。用户接口定义用户与系统交互的方式，外部接口定义系统与外部服务（如Python审核服务）的交互，内部接口定义系统内部各模块之间的接口规范。

### 3.2.1 用户接口设计

用户接口是指用户通过浏览器与系统进行交互的接口，主要包括页面操作、表单输入、数据展示等方式。

#### 3.2.1.1 登录接口

**接口描述**：用户通过登录页面输入用户名和密码进行身份认证。

**操作步骤**：
1. 用户访问系统登录页面
2. 用户在"用户名"输入框中输入用户名
3. 用户在"密码"输入框中输入密码
4. 用户点击"登录"按钮
5. 系统验证用户信息，成功后跳转到对应角色的工作台

**输入格式**：
- 用户名：文本类型，必填，长度3-50个字符
- 密码：密码类型，必填，长度6-100个字符

**响应信息**：
- 登录成功：跳转到角色对应的工作台页面，页面显示欢迎信息
- 登录失败：显示错误提示信息（如"用户名或密码错误"）

#### 3.2.1.2 导航菜单接口

**接口描述**：系统根据用户角色显示不同的导航菜单。

**操作说明**：
- 管理员菜单：仪表板、药品管理、库存管理、用户管理、处方管理、审核记录、系统设置
- 医生菜单：工作台、我的处方
- 护士菜单：工作台、处方查询
- 药师菜单：工作台、处方审核、库存管理、审核记录

**操作方式**：用户点击菜单项，系统跳转到对应的功能页面。

#### 3.2.1.3 数据表格接口

**接口描述**：系统以表格形式展示数据列表，支持分页、排序、筛选、搜索等功能。

**操作方式**：
- **分页**：点击页码或上一页/下一页按钮切换页码，显示页码输入框可跳转到指定页
- **排序**：点击表头可对当前列进行升序/降序排序
- **筛选**：使用筛选下拉框选择筛选条件，点击"筛选"按钮应用筛选
- **搜索**：在搜索输入框中输入关键词，点击"搜索"按钮或按Enter键执行搜索
- **查看详情**：点击表格中的"查看"按钮或行数据，打开详情对话框或跳转到详情页面
- **编辑**：点击"编辑"按钮，打开编辑表单对话框
- **删除**：点击"删除"按钮，弹出确认对话框，确认后执行删除操作

**响应格式**：
- 表格数据以行的形式展示，每行包含相关的数据字段
- 表格底部显示分页信息（当前页/总页数、总记录数）
- 操作按钮位于表格的操作列中

#### 3.2.1.4 表单输入接口

**接口描述**：系统提供多种类型的表单输入控件，用于数据录入和编辑。

**输入控件类型**：
- **文本输入框**：用于输入文本信息（如用户名、药品名称等）
- **密码输入框**：用于输入密码，输入内容自动隐藏
- **数字输入框**：用于输入数字（如价格、数量等），支持最小值、最大值限制
- **日期选择器**：用于选择日期，点击输入框弹出日历选择器
- **下拉选择框**：用于从预定义选项中选择（如角色、性别、出库原因等）
- **多行文本框**：用于输入较长的文本（如适应症、禁忌症等）
- **文件上传**：用于上传文件（可选功能）

**表单验证**：
- **必填验证**：必填字段为空时，显示红色边框和错误提示信息
- **格式验证**：输入格式不正确时（如邮箱格式、数字格式），显示格式错误提示
- **长度验证**：输入长度超出限制时，显示长度错误提示
- **唯一性验证**：输入值重复时（如用户名、处方号），显示重复错误提示

**表单提交**：
- 点击"保存"或"提交"按钮提交表单
- 提交前进行表单验证，验证通过后才发送请求
- 提交过程中显示加载状态，禁用提交按钮防止重复提交
- 提交成功后显示成功提示，并关闭表单对话框或跳转页面

#### 3.2.1.5 确认对话框接口

**接口描述**：系统在执行删除、取消等关键操作前，弹出确认对话框要求用户确认。

**对话框内容**：
- 标题：如"确认删除"、"确认取消"
- 提示信息：如"您确认要删除这条记录吗？此操作不可恢复！"
- 按钮：取消按钮、确认按钮

**操作方式**：
- 点击"取消"按钮：关闭对话框，不执行操作
- 点击"确认"按钮：关闭对话框，执行操作

#### 3.2.1.6 消息提示接口

**接口描述**：系统通过消息提示向用户反馈操作结果和系统状态。

**提示类型**：
- **成功提示**：操作成功时显示，绿色背景，显示成功图标和提示信息（如"保存成功"）
- **错误提示**：操作失败时显示，红色背景，显示错误图标和错误信息（如"保存失败：XXX"）
- **警告提示**：需要用户注意时显示，黄色背景，显示警告图标和警告信息（如"库存不足"）
- **信息提示**：一般信息提示时显示，蓝色背景，显示信息图标和信息内容

**显示方式**：
- 提示消息在页面右上角或顶部显示，自动消失（默认3秒后消失）
- 用户可以手动关闭提示消息

### 3.2.2 外部接口设计

系统与外部系统的接口主要包括与Python审核服务的接口，以及与未来可能集成的HIS系统的预留接口。

#### 3.2.2.1 Python审核服务接口

**接口描述**：Java后端通过HTTP RESTful API调用Python审核服务，进行处方自动审核。

**接口基本信息**：
- **接口地址**：`http://localhost:5000/api/prescription/audit`（可配置）
- **请求方法**：POST
- **协议**：HTTP/HTTPS
- **数据格式**：JSON
- **字符编码**：UTF-8
- **超时时间**：5秒

**请求消息格式**：

**请求头（Request Headers）**：
```
Content-Type: application/json
Accept: application/json
```

**请求体（Request Body）**：
```json
{
  "prescription": {
    "prescriptionNumber": "RX20241212001",
    "patientName": "张三",
    "patientAge": 45,
    "patientGender": "男",
    "patientSymptoms": "发热、咳嗽",
    "diagnosis": "上呼吸道感染",
    "allergies": "无"
  },
  "medicines": [
    {
      "medicineId": 1,
      "medicineName": "阿莫西林胶囊",
      "indication": "用于敏感菌所致的各种感染",
      "contraindication": "对青霉素类药物过敏者禁用",
      "interactions": "与丙磺舒合用可提高血药浓度",
      "quantity": 2,
      "dosage": "0.5g",
      "frequency": "每日3次",
      "days": 5
    }
  ]
}
```

**请求字段说明**：

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| prescription | Object | 是 | 处方基本信息 |
| prescription.prescriptionNumber | String | 是 | 处方编号 |
| prescription.patientName | String | 是 | 患者姓名 |
| prescription.patientAge | Integer | 是 | 患者年龄 |
| prescription.patientGender | String | 是 | 患者性别 |
| prescription.patientSymptoms | String | 否 | 患者症状/病症 |
| prescription.diagnosis | String | 否 | 诊断 |
| prescription.allergies | String | 否 | 过敏史 |
| medicines | Array | 是 | 药品明细列表 |
| medicines[].medicineId | Integer | 是 | 药品ID |
| medicines[].medicineName | String | 是 | 药品名称 |
| medicines[].indication | String | 否 | 适应症 |
| medicines[].contraindication | String | 否 | 禁忌症 |
| medicines[].interactions | String | 否 | 药物相互作用 |
| medicines[].quantity | Integer | 是 | 数量 |
| medicines[].dosage | String | 是 | 用法用量 |
| medicines[].frequency | String | 是 | 频次 |
| medicines[].days | Integer | 是 | 天数 |

**响应消息格式**：

**响应头（Response Headers）**：
```
Content-Type: application/json
```

**响应体（Response Body）**：
```json
{
  "success": true,
  "message": "审核完成",
  "data": {
    "result": "pass",
    "score": 85,
    "issues": [],
    "suggestions": "建议注意用药时间间隔"
  }
}
```

**响应字段说明**：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| success | Boolean | 请求是否成功 |
| message | String | 响应消息 |
| data | Object | 审核结果数据 |
| data.result | String | 审核结果：pass（通过）、warning（警告）、reject（拒绝） |
| data.score | Integer | 审核评分：0-100分 |
| data.issues | Array | 发现的问题列表 |
| data.suggestions | String | 审核建议 |

**错误响应格式**：
```json
{
  "success": false,
  "message": "错误描述信息",
  "data": null
}
```

**接口调用流程**：
1. Java后端接收到处方提交审核请求
2. 后端构建审核请求数据
3. 后端通过RestTemplate发送POST请求到Python服务
4. Python服务执行审核算法，返回审核结果
5. 后端接收审核结果，保存审核记录
6. 后端更新处方状态

#### 3.2.2.2 HIS系统对接接口（预留）

**接口描述**：预留与医院信息系统（HIS）对接的接口，用于同步患者信息、药品信息等。

**接口类型**：RESTful API或Web Service

**预留接口列表**：
- 同步患者信息接口
- 同步药品信息接口
- 同步处方信息接口

**说明**：当前版本未实现，作为扩展功能预留接口。

### 3.2.3 内部接口设计

内部接口定义了系统内部各模块之间的接口规范，包括Controller层与Service层之间的接口、Service层与Repository层之间的接口等。

#### 3.2.3.1 Controller层接口规范

**接口描述**：Controller层接收HTTP请求，调用Service层业务逻辑，返回统一格式的响应。

**统一请求格式**：
- **请求方法**：GET（查询）、POST（创建）、PUT（更新）、DELETE（删除）
- **请求路径**：RESTful风格，如 `/api/medicines`、`/api/prescriptions/{id}`
- **请求参数**：
  - URL参数：用于查询条件（如 `?page=1&size=10&keyword=xxx`）
  - 路径参数：用于资源标识（如 `/prescriptions/{id}`）
  - 请求体：JSON格式，用于创建和更新操作
- **认证信息**：通过请求头 `Authorization: Bearer {token}` 传递JWT令牌

**统一响应格式**：

**成功响应**：
```json
{
  "success": true,
  "message": "操作成功",
  "data": {
    // 响应数据
  }
}
```

**分页响应**：
```json
{
  "success": true,
  "message": "查询成功",
  "data": {
    "content": [
      // 数据列表
    ],
    "totalElements": 100,
    "totalPages": 10,
    "page": 1,
    "size": 10
  }
}
```

**错误响应**：
```json
{
  "success": false,
  "message": "错误描述信息",
  "data": null
}
```

**HTTP状态码**：
- 200：请求成功
- 201：创建成功
- 400：请求参数错误
- 401：未认证
- 403：权限不足
- 404：资源不存在
- 500：服务器内部错误

#### 3.2.3.2 Service层接口规范

**接口描述**：Service层实现业务逻辑，调用Repository层进行数据访问。

**方法命名规范**：
- 查询方法：`find*`、`get*`、`query*`
- 创建方法：`create*`、`save*`
- 更新方法：`update*`、`modify*`
- 删除方法：`delete*`、`remove*`

**参数传递规范**：
- 使用DTO（Data Transfer Object）传递参数
- 使用实体类（Entity）进行数据持久化
- 复杂查询使用Map或自定义查询对象传递查询条件

**返回值规范**：
- 查询方法返回实体对象或实体对象列表
- 创建/更新方法返回保存后的实体对象
- 删除方法返回void或Boolean
- 异常情况抛出业务异常（BusinessException）

**事务管理**：
- 使用`@Transactional`注解管理事务
- 关键业务操作（如处方创建、审核、发药）必须使用事务保证数据一致性

#### 3.2.3.3 Repository层接口规范

**接口描述**：Repository层使用JPA进行数据访问，提供数据持久化功能。

**接口定义规范**：
- 继承`JpaRepository<Entity, ID>`接口
- 使用Spring Data JPA的查询方法命名规范
- 复杂查询使用`@Query`注解定义JPQL或SQL查询

**查询方法示例**：
```java
// 根据用户名查找用户
Optional<User> findByUsername(String username);

// 根据处方号查找处方
Optional<Prescription> findByPrescriptionNumber(String prescriptionNumber);

// 根据药品名称模糊查询
List<Medicine> findByNameContaining(String name);

// 根据处方状态查询
List<Prescription> findByStatus(String status);

// 自定义查询
@Query("SELECT p FROM Prescription p WHERE p.doctorName = :doctorName AND p.status = :status")
List<Prescription> findByDoctorAndStatus(@Param("doctorName") String doctorName, @Param("status") String status);
```

#### 3.2.3.4 模块间调用接口

**（1）处方管理模块调用药品管理模块**

**接口描述**：处方创建和查询时需要获取药品信息。

**调用方式**：
```java
// PrescriptionService调用MedicineService
MedicineService.getById(medicineId);  // 根据药品ID获取药品信息
MedicineService.findAll();            // 获取所有药品列表
```

**参数**：
- medicineId：药品ID（Long类型）

**返回值**：Medicine实体对象

**（2）处方管理模块调用库存管理模块**

**接口描述**：处方发药时需要扣减库存。

**调用方式**：
```java
// PrescriptionService调用StockOutService
StockOutService.processStockOut(stockOut);  // 处理出库，自动扣减库存
```

**参数**：
- stockOut：StockOut对象，包含药品ID、出库数量、出库原因等信息

**返回值**：void（处理成功）或抛出StockInsufficientException（库存不足）

**（3）审核管理模块调用处方管理模块**

**接口描述**：审核时需要获取处方信息和处方明细。

**调用方式**：
```java
// AuditService调用PrescriptionService
PrescriptionService.findById(prescriptionId);        // 获取处方信息
PrescriptionDetailService.findByPrescriptionId(id);  // 获取处方明细
```

**参数**：
- prescriptionId：处方ID（Long类型）

**返回值**：Prescription对象和PrescriptionDetail列表

**（4）审核管理模块调用药品管理模块**

**接口描述**：审核时需要获取药品的适应症、禁忌症等信息。

**调用方式**：
```java
// AuditService调用MedicineService
MedicineService.getById(medicineId);  // 获取药品详细信息
```

**参数**：
- medicineId：药品ID（Long类型）

**返回值**：Medicine对象，包含适应症、禁忌症、相互作用等信息

#### 3.2.3.5 RESTful API接口详细定义

本小节列出系统主要的RESTful API接口定义，包括请求方法、路径、参数和响应格式。

##### 表3.6 用户认证相关API接口

| 接口路径 | 请求方法 | 功能描述 | 权限要求 | 请求参数 | 响应数据 |
|---------|---------|---------|---------|---------|---------|
| /api/auth/login | POST | 用户登录 | 无需认证 | LoginRequest<br/>{username, password} | LoginResponse<br/>{token, userInfo} |
| /api/auth/register | POST | 用户注册 | 无需认证 | RegisterRequest<br/>{username, password, role, ...} | UserResponse |
| /api/auth/me | GET | 获取当前用户信息 | 需要认证 | 无 | UserResponse |
| /api/auth/refresh | POST | 刷新令牌 | 需要认证 | RefreshTokenRequest<br/>{token} | LoginResponse |
| /api/auth/logout | POST | 用户登出 | 需要认证 | 无 | ApiResponse |

##### 表3.7 用户管理相关API接口

| 接口路径 | 请求方法 | 功能描述 | 权限要求 | 请求参数 | 响应数据 |
|---------|---------|---------|---------|---------|---------|
| /api/users | GET | 获取用户列表 | admin | page, size, keyword | PageResponse<UserResponse> |
| /api/users | POST | 创建用户 | admin | RegisterRequest | UserResponse |
| /api/users/{id} | GET | 获取用户详情 | admin | id（路径参数） | UserResponse |
| /api/users/{id} | PUT | 更新用户信息 | admin | id, UserUpdateRequest | UserResponse |
| /api/users/{id} | DELETE | 删除用户 | admin | id（路径参数） | ApiResponse |
| /api/users/{id}/role | PUT | 修改用户角色 | admin | id, role | ApiResponse |

##### 表3.8 药品管理相关API接口

| 接口路径 | 请求方法 | 功能描述 | 权限要求 | 请求参数 | 响应数据 |
|---------|---------|---------|---------|---------|---------|
| /api/medicines | GET | 获取药品列表 | 所有用户 | page, size, keyword, category | PageResponse<MedicineResponse> |
| /api/medicines | POST | 创建药品 | admin, pharmacist | MedicineCreateRequest | MedicineResponse |
| /api/medicines/{id} | GET | 获取药品详情 | 所有用户 | id（路径参数） | MedicineResponse |
| /api/medicines/{id} | PUT | 更新药品信息 | admin, pharmacist | id, MedicineUpdateRequest | MedicineResponse |
| /api/medicines/{id} | DELETE | 删除药品 | admin, pharmacist | id（路径参数） | ApiResponse |
| /api/medicines/search | GET | 搜索药品 | 所有用户 | keyword | List<MedicineResponse> |
| /api/medicines/category/{category} | GET | 按分类查询药品 | 所有用户 | category（路径参数） | List<MedicineResponse> |
| /api/medicines/low-stock | GET | 查询低库存药品 | 所有用户 | 无 | List<MedicineResponse> |

##### 表3.9 库存管理相关API接口

| 接口路径 | 请求方法 | 功能描述 | 权限要求 | 请求参数 | 响应数据 |
|---------|---------|---------|---------|---------|---------|
| /api/stock/in | POST | 药品入库 | admin, pharmacist | StockInRequest<br/>{medicineId, quantity, batchNumber, supplier, inDate} | StockInResponse |
| /api/stock/out | POST | 药品出库 | admin, pharmacist | StockOutRequest<br/>{medicineId, quantity, reason, outDate} | StockOutResponse |
| /api/stock/in | GET | 获取入库记录列表 | 所有用户 | page, size, medicineId, startDate, endDate | PageResponse<StockInResponse> |
| /api/stock/out | GET | 获取出库记录列表 | 所有用户 | page, size, medicineId, reason, startDate, endDate | PageResponse<StockOutResponse> |
| /api/stock/medicine/{medicineId} | GET | 查询药品库存 | 所有用户 | medicineId（路径参数） | {stockQuantity, minStock, isLowStock} |
| /api/stock/statistics | GET | 库存统计 | 所有用户 | 无 | {totalMedicines, lowStockCount, totalValue} |

##### 表3.10 处方管理相关API接口

| 接口路径 | 请求方法 | 功能描述 | 权限要求 | 请求参数 | 响应数据 |
|---------|---------|---------|---------|---------|---------|
| /api/prescriptions | POST | 创建处方 | doctor | PrescriptionCreateRequest<br/>{patientInfo, details[]} | PrescriptionResponse |
| /api/prescriptions | GET | 获取处方列表 | 所有用户 | page, size, status, doctorName, patientName | PageResponse<PrescriptionResponse> |
| /api/prescriptions/{id} | GET | 获取处方详情 | 所有用户 | id（路径参数） | PrescriptionResponse |
| /api/prescriptions/{id} | PUT | 更新处方信息 | doctor | id, PrescriptionUpdateRequest | PrescriptionResponse |
| /api/prescriptions/{id}/submit-audit | POST | 提交审核 | doctor | id（路径参数） | ApiResponse |
| /api/prescriptions/{id}/audit | POST | 人工审核 | pharmacist | id, PrescriptionAuditRequest<br/>{result, suggestions} | ApiResponse |
| /api/prescriptions/{id}/dispense | POST | 发药 | nurse | id（路径参数） | ApiResponse |
| /api/prescriptions/{id}/cancel | POST | 取消处方 | doctor | id（路径参数） | ApiResponse |
| /api/prescriptions/{id}/details | GET | 获取处方明细 | 所有用户 | id（路径参数） | List<PrescriptionDetailResponse> |
| /api/prescriptions/{id}/audit-history | GET | 获取审核历史 | 所有用户 | id（路径参数） | List<AuditRecordResponse> |

##### 表3.11 审核记录相关API接口

| 接口路径 | 请求方法 | 功能描述 | 权限要求 | 请求参数 | 响应数据 |
|---------|---------|---------|---------|---------|---------|
| /api/audit/records | GET | 获取审核记录列表 | admin, pharmacist | page, size, prescriptionId, auditType, result | PageResponse<AuditRecordResponse> |
| /api/audit/records/{id} | GET | 获取审核记录详情 | admin, pharmacist | id（路径参数） | AuditRecordResponse |
| /api/audit/records/prescription/{prescriptionId} | GET | 获取处方的审核历史 | admin, pharmacist | prescriptionId（路径参数） | List<AuditRecordResponse> |
| /api/audit/statistics | GET | 审核统计 | admin, pharmacist | startDate, endDate | {totalAudits, passCount, rejectCount, warningCount, passRate} |

#### 3.2.3.6 数据传递对象（DTO）规范

**DTO设计原则**：
- 使用DTO在层与层之间传递数据，避免直接暴露实体类
- 请求DTO用于接收前端数据，响应DTO用于返回给前端
- DTO只包含需要传输的字段，不包含业务逻辑

**主要DTO类**：

**（1）通用响应DTO**：
- `ApiResponse<T>`：统一API响应格式，包含success、message、data字段
- `PageResponse<T>`：分页响应格式，包含content、totalElements、totalPages、page、size字段

**（2）认证相关DTO**：
- `LoginRequest`：登录请求，包含username、password
- `RegisterRequest`：注册请求，包含username、password、role、realName等
- `LoginResponse`：登录响应，包含token、userInfo

**（3）用户相关DTO**：
- `UserResponse`：用户信息响应，包含id、username、role、realName、department等

**（4）药品相关DTO**：
- `MedicineCreateRequest`：创建药品请求
- `MedicineUpdateRequest`：更新药品请求
- `MedicineResponse`：药品信息响应

**（5）处方相关DTO**：
- `PrescriptionCreateRequest`：创建处方请求
- `PrescriptionDetailRequest`：处方明细请求
- `PrescriptionAuditRequest`：审核请求
- `PrescriptionResponse`：处方信息响应
- `PrescriptionDetailResponse`：处方明细响应

**（6）库存相关DTO**：
- `StockInRequest`：入库请求
- `StockOutRequest`：出库请求
- `StockInResponse`：入库记录响应
- `StockOutResponse`：出库记录响应

**（7）审核相关DTO**：
- `AuditRecordResponse`：审核记录响应
- `AuditResultDto`：审核结果DTO（Python服务返回）

## 3.3 数据模型设计

数据模型设计包括概念数据模型、数据库表设计和数据文件设计三个部分。概念数据模型描述了现实世界中的实体、属性和它们之间的关系；数据库表设计建立了系统程序员视图；数据文件设计定义了系统中用于数据传输的文件结构。

### 3.3.1 概念数据模型

概念数据模型用于描述问题域中的核心概念、概念之间的关系以及概念的重要属性。本系统的概念数据模型包含7个核心实体，它们之间的关系构成了系统的数据基础。

#### 3.3.1.1 实体关系图（ER图）

**ER图元素说明：**
（注：用户可自行绘制ER图，以下为图的元素描述）

```
实体（矩形框）：
1. User（用户）
   - 属性：id, username, password, role, realName, employeeNumber, title, department, createTime, updateTime
   - 主键：id

2. Medicine（药品）
   - 属性：id, name, genericName, specification, manufacturer, price, stockQuantity, minStock, category, approvalNumber, indication, contraindication, interactions, createTime, updateTime
   - 主键：id

3. Prescription（处方）
   - 属性：id, prescriptionNumber, patientName, patientAge, patientGender, patientSymptoms, diagnosis, patientConditions, allergies, doctorName, department, createDate, status, auditResult, auditTime, createTime, updateTime
   - 主键：id
   - 唯一键：prescriptionNumber

4. PrescriptionDetail（处方明细）
   - 属性：id, prescriptionId, medicineId, quantity, dosage, frequency, days, createTime
   - 主键：id

5. StockIn（入库记录）
   - 属性：id, medicineId, batchNumber, quantity, supplier, inDate, operator, createTime
   - 主键：id

6. StockOut（出库记录）
   - 属性：id, medicineId, batchNumber, quantity, outDate, operator, reason, createTime
   - 主键：id

7. AuditRecord（审核记录）
   - 属性：id, prescriptionId, auditType, auditResult, auditScore, issuesFound, suggestions, auditor, auditTime, createTime
   - 主键：id

关系（菱形框）：
1. Prescription --[1:N]--> PrescriptionDetail
   - 关系名：包含
   - 说明：一个处方可以包含多个处方明细，一个处方明细只属于一个处方

2. PrescriptionDetail --[N:1]--> Medicine
   - 关系名：引用
   - 说明：多个处方明细可以引用同一个药品，一个处方明细只引用一个药品

3. StockIn --[N:1]--> Medicine
   - 关系名：入库
   - 说明：多个入库记录可以对应同一个药品，一个入库记录只对应一个药品

4. StockOut --[N:1]--> Medicine
   - 关系名：出库
   - 说明：多个出库记录可以对应同一个药品，一个出库记录只对应一个药品

5. AuditRecord --[N:1]--> Prescription
   - 关系名：审核
   - 说明：多个审核记录可以对应同一个处方，一个审核记录只对应一个处方

ER图绘制说明：
- 使用矩形框表示实体
- 使用菱形框表示关系
- 使用直线连接实体和关系
- 在连接线上标注关系的基数（1:N, N:1等）
- 实体属性可以列出，也可以用下划线标注主键属性
```

#### 3.3.1.2 数据字典

**（1）用户实体（User）数据字典**

| 属性名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 说明 |
|--------|---------|------|---------|---------|------|
| id | BIGINT | - | 是 | 否 | 用户ID，自增主键 |
| username | VARCHAR | 50 | 否 | 否 | 用户名，唯一，用于登录 |
| password | VARCHAR | 255 | 否 | 否 | 密码，BCrypt加密存储 |
| role | VARCHAR | 20 | 否 | 否 | 用户角色：admin（管理员）、doctor（医生）、nurse（护士）、pharmacist（药师） |
| realName | VARCHAR | 50 | 否 | 是 | 真实姓名 |
| employeeNumber | VARCHAR | 50 | 否 | 是 | 职工号，唯一 |
| title | VARCHAR | 50 | 否 | 是 | 职称，如：主任医师、副主任医师、主治医师、住院医师、主管药师、药师等 |
| department | VARCHAR | 100 | 否 | 是 | 所属部门 |
| createTime | TIMESTAMP | - | 否 | 否 | 创建时间，自动生成 |
| updateTime | TIMESTAMP | - | 否 | 否 | 更新时间，自动更新 |

**（2）药品实体（Medicine）数据字典**

| 属性名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 说明 |
|--------|---------|------|---------|---------|------|
| id | BIGINT | - | 是 | 否 | 药品ID，自增主键 |
| name | VARCHAR | 100 | 否 | 否 | 药品名称 |
| genericName | VARCHAR | 100 | 否 | 是 | 通用名 |
| specification | VARCHAR | 50 | 否 | 是 | 规格，如：0.25g*24粒 |
| manufacturer | VARCHAR | 100 | 否 | 是 | 生产厂家 |
| price | DECIMAL | 10,2 | 否 | 否 | 价格，单位：元，精确到分 |
| stockQuantity | INT | - | 否 | 否 | 库存数量，默认0 |
| minStock | INT | - | 否 | 是 | 最低库存预警值，默认10 |
| category | VARCHAR | 50 | 否 | 是 | 药品分类，如：抗生素、解热镇痛、维生素等 |
| approvalNumber | VARCHAR | 50 | 否 | 是 | 批准文号 |
| indication | TEXT | - | 否 | 是 | 适应症 |
| contraindication | TEXT | - | 否 | 是 | 禁忌症 |
| interactions | TEXT | - | 否 | 是 | 药物相互作用 |
| createTime | TIMESTAMP | - | 否 | 否 | 创建时间，自动生成 |
| updateTime | TIMESTAMP | - | 否 | 否 | 更新时间，自动更新 |

**（3）处方实体（Prescription）数据字典**

| 属性名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 说明 |
|--------|---------|------|---------|---------|------|
| id | BIGINT | - | 是 | 否 | 处方ID，自增主键 |
| prescriptionNumber | VARCHAR | 50 | 否 | 否 | 处方编号，唯一，格式：RX+日期+序号 |
| patientName | VARCHAR | 50 | 否 | 否 | 患者姓名 |
| patientAge | INT | - | 否 | 是 | 患者年龄 |
| patientGender | VARCHAR | 20 | 否 | 是 | 患者性别：男、女 |
| patientSymptoms | TEXT | - | 否 | 是 | 患者症状/病症 |
| diagnosis | VARCHAR | 200 | 否 | 是 | 诊断 |
| patientConditions | TEXT | - | 否 | 是 | 患者疾病状况 |
| allergies | TEXT | - | 否 | 是 | 过敏史 |
| doctorName | VARCHAR | 50 | 否 | 是 | 医生姓名 |
| department | VARCHAR | 50 | 否 | 是 | 开方科室 |
| createDate | DATE | - | 否 | 否 | 处方日期 |
| status | VARCHAR | 20 | 否 | 否 | 处方状态：未审核、审核中、已通过、已拒绝、已发药、已取消 |
| auditResult | TEXT | - | 否 | 是 | 审核结果描述 |
| auditTime | TIMESTAMP | - | 否 | 是 | 审核时间 |
| createTime | TIMESTAMP | - | 否 | 否 | 创建时间，自动生成 |
| updateTime | TIMESTAMP | - | 否 | 否 | 更新时间，自动更新 |

**（4）处方明细实体（PrescriptionDetail）数据字典**

| 属性名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 说明 |
|--------|---------|------|---------|---------|------|
| id | BIGINT | - | 是 | 否 | 明细ID，自增主键 |
| prescriptionId | BIGINT | - | 否 | 否 | 处方ID，外键关联prescription.id |
| medicineId | BIGINT | - | 否 | 否 | 药品ID，外键关联medicine.id |
| quantity | INT | - | 否 | 否 | 数量，必须大于0 |
| dosage | VARCHAR | 100 | 否 | 是 | 用法用量，如：每次2粒 |
| frequency | VARCHAR | 50 | 否 | 是 | 频次，如：每日3次 |
| days | INT | - | 否 | 是 | 用药天数 |
| createTime | TIMESTAMP | - | 否 | 否 | 创建时间，自动生成 |

**（5）入库记录实体（StockIn）数据字典**

| 属性名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 说明 |
|--------|---------|------|---------|---------|------|
| id | BIGINT | - | 是 | 否 | 入库记录ID，自增主键 |
| medicineId | BIGINT | - | 否 | 否 | 药品ID，外键关联medicine.id |
| batchNumber | VARCHAR | 50 | 否 | 是 | 批次号 |
| quantity | INT | - | 否 | 否 | 入库数量，必须大于0 |
| supplier | VARCHAR | 100 | 否 | 是 | 供应商 |
| inDate | DATE | - | 否 | 否 | 入库日期 |
| operator | VARCHAR | 50 | 否 | 是 | 操作员 |
| createTime | TIMESTAMP | - | 否 | 否 | 创建时间，自动生成 |

**（6）出库记录实体（StockOut）数据字典**

| 属性名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 说明 |
|--------|---------|------|---------|---------|------|
| id | BIGINT | - | 是 | 否 | 出库记录ID，自增主键 |
| medicineId | BIGINT | - | 否 | 否 | 药品ID，外键关联medicine.id |
| batchNumber | VARCHAR | 50 | 否 | 是 | 批次号 |
| quantity | INT | - | 否 | 否 | 出库数量，必须大于0 |
| outDate | DATE | - | 否 | 否 | 出库日期 |
| operator | VARCHAR | 50 | 否 | 是 | 操作员 |
| reason | ENUM | - | 否 | 是 | 出库原因：prescription（处方发药）、loss（盘亏）、expired（过期）、other（其他） |
| createTime | TIMESTAMP | - | 否 | 否 | 创建时间，自动生成 |

**（7）审核记录实体（AuditRecord）数据字典**

| 属性名 | 数据类型 | 长度 | 是否主键 | 是否为空 | 说明 |
|--------|---------|------|---------|---------|------|
| id | BIGINT | - | 是 | 否 | 审核记录ID，自增主键 |
| prescriptionId | BIGINT | - | 否 | 否 | 处方ID，外键关联prescription.id |
| auditType | ENUM | - | 否 | 否 | 审核类型：auto（自动审核）、manual（人工审核），默认auto |
| auditResult | ENUM | - | 否 | 否 | 审核结果：pass（通过）、warning（警告）、reject（拒绝） |
| auditScore | DECIMAL | 5,2 | 否 | 是 | 审核评分，0-100分 |
| issuesFound | TEXT | - | 否 | 是 | 发现的问题 |
| suggestions | TEXT | - | 否 | 是 | 审核建议 |
| auditor | VARCHAR | 50 | 否 | 是 | 审核人，自动审核时填写"系统审核" |
| auditTime | TIMESTAMP | - | 否 | 否 | 审核时间，自动生成 |
| createTime | TIMESTAMP | - | 否 | 否 | 创建时间，自动生成 |

### 3.3.2 数据库表设计

数据库表设计是基于概念数据模型建立的系统程序员视图，使用MySQL 8.0数据库，采用JPA/Hibernate进行ORM映射。本系统共设计7张核心数据表。

#### 3.3.2.1 用户表（users）

**表说明**：存储系统用户信息，包括管理员、医生、护士、药师等不同角色的用户。

**表结构**：

| 列名 | 数据类型 | 长度 | 约束 | 默认值 | 说明 |
|------|---------|------|------|--------|------|
| id | BIGINT | - | PRIMARY KEY, AUTO_INCREMENT | - | 用户ID |
| username | VARCHAR | 50 | NOT NULL, UNIQUE | - | 用户名 |
| password | VARCHAR | 255 | NOT NULL | - | 密码（BCrypt加密） |
| role | VARCHAR | 20 | NOT NULL | 'admin' | 用户角色 |
| real_name | VARCHAR | 50 | NULL | NULL | 真实姓名 |
| employee_number | VARCHAR | 50 | UNIQUE | NULL | 职工号 |
| title | VARCHAR | 50 | NULL | NULL | 职称 |
| department | VARCHAR | 50 | NULL | NULL | 部门 |
| create_time | TIMESTAMP | - | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |
| update_time | TIMESTAMP | - | NOT NULL | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引**：
- PRIMARY KEY (id)
- UNIQUE KEY uk_username (username)
- UNIQUE KEY uk_employee_number (employee_number)

#### 3.3.2.2 药品表（medicine）

**表说明**：存储药品基本信息、专业信息和库存信息。

**表结构**：

| 列名 | 数据类型 | 长度 | 约束 | 默认值 | 说明 |
|------|---------|------|------|--------|------|
| id | BIGINT | - | PRIMARY KEY, AUTO_INCREMENT | - | 药品ID |
| name | VARCHAR | 100 | NOT NULL | - | 药品名称 |
| generic_name | VARCHAR | 100 | NULL | NULL | 通用名 |
| specification | VARCHAR | 50 | NULL | NULL | 规格 |
| manufacturer | VARCHAR | 100 | NULL | NULL | 生产厂家 |
| price | DECIMAL | 10,2 | NOT NULL | 0.00 | 价格（元） |
| stock_quantity | INT | - | NOT NULL | 0 | 库存数量 |
| min_stock | INT | - | NULL | 10 | 最低库存预警值 |
| category | VARCHAR | 50 | NULL | NULL | 药品分类 |
| approval_number | VARCHAR | 50 | NULL | NULL | 批准文号 |
| indication | TEXT | - | NULL | NULL | 适应症 |
| contraindication | TEXT | - | NULL | NULL | 禁忌症 |
| interactions | TEXT | - | NULL | NULL | 药物相互作用 |
| create_time | TIMESTAMP | - | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |
| update_time | TIMESTAMP | - | NOT NULL | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引**：
- PRIMARY KEY (id)
- INDEX idx_category (category) - 用于按分类查询

#### 3.3.2.3 处方表（prescription）

**表说明**：存储处方主表信息，包含患者信息和处方状态。

**表结构**：

| 列名 | 数据类型 | 长度 | 约束 | 默认值 | 说明 |
|------|---------|------|------|--------|------|
| id | BIGINT | - | PRIMARY KEY, AUTO_INCREMENT | - | 处方ID |
| prescription_number | VARCHAR | 50 | NOT NULL, UNIQUE | - | 处方编号 |
| patient_name | VARCHAR | 50 | NOT NULL | - | 患者姓名 |
| patient_age | INT | - | NULL | NULL | 患者年龄 |
| patient_gender | VARCHAR | 20 | NULL | NULL | 患者性别 |
| patient_symptoms | TEXT | - | NULL | NULL | 患者症状/病症 |
| diagnosis | VARCHAR | 200 | NULL | NULL | 诊断 |
| patient_conditions | TEXT | - | NULL | NULL | 患者疾病状况 |
| allergies | TEXT | - | NULL | NULL | 过敏史 |
| doctor_name | VARCHAR | 50 | NULL | NULL | 医生姓名 |
| department | VARCHAR | 50 | NULL | NULL | 科室 |
| create_date | DATE | - | NOT NULL | - | 处方日期 |
| status | VARCHAR | 20 | NOT NULL | '未审核' | 处方状态 |
| audit_result | TEXT | - | NULL | NULL | 审核结果 |
| audit_time | TIMESTAMP | - | NULL | NULL | 审核时间 |
| create_time | TIMESTAMP | - | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |
| update_time | TIMESTAMP | - | NOT NULL | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引**：
- PRIMARY KEY (id)
- UNIQUE KEY uk_prescription_number (prescription_number)
- INDEX idx_status (status) - 用于按状态查询
- INDEX idx_doctor_name (doctor_name) - 用于按医生查询
- INDEX idx_create_date (create_date) - 用于按日期查询

#### 3.3.2.4 处方明细表（prescription_detail）

**表说明**：存储处方明细信息，一个处方可以包含多个处方明细。

**表结构**：

| 列名 | 数据类型 | 长度 | 约束 | 默认值 | 说明 |
|------|---------|------|------|--------|------|
| id | BIGINT | - | PRIMARY KEY, AUTO_INCREMENT | - | 明细ID |
| prescription_id | BIGINT | - | NOT NULL, FOREIGN KEY | - | 处方ID，外键关联prescription.id |
| medicine_id | BIGINT | - | NOT NULL, FOREIGN KEY | - | 药品ID，外键关联medicine.id |
| quantity | INT | - | NOT NULL | - | 数量 |
| dosage | VARCHAR | 100 | NULL | NULL | 用法用量 |
| frequency | VARCHAR | 50 | NULL | NULL | 频次 |
| days | INT | - | NULL | NULL | 用药天数 |
| create_time | TIMESTAMP | - | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |

**索引**：
- PRIMARY KEY (id)
- FOREIGN KEY fk_prescription_detail_prescription (prescription_id) REFERENCES prescription(id)
- FOREIGN KEY fk_prescription_detail_medicine (medicine_id) REFERENCES medicine(id)
- INDEX idx_prescription_id (prescription_id) - 用于查询处方的所有明细

#### 3.3.2.5 入库记录表（stock_in）

**表说明**：存储药品入库记录信息。

**表结构**：

| 列名 | 数据类型 | 长度 | 约束 | 默认值 | 说明 |
|------|---------|------|------|--------|------|
| id | BIGINT | - | PRIMARY KEY, AUTO_INCREMENT | - | 入库记录ID |
| medicine_id | BIGINT | - | NOT NULL, FOREIGN KEY | - | 药品ID，外键关联medicine.id |
| batch_number | VARCHAR | 50 | NULL | NULL | 批次号 |
| quantity | INT | - | NOT NULL | - | 入库数量 |
| supplier | VARCHAR | 100 | NULL | NULL | 供应商 |
| in_date | DATE | - | NOT NULL | - | 入库日期 |
| operator | VARCHAR | 50 | NULL | NULL | 操作员 |
| create_time | TIMESTAMP | - | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |

**索引**：
- PRIMARY KEY (id)
- FOREIGN KEY fk_stock_in_medicine (medicine_id) REFERENCES medicine(id)
- INDEX idx_medicine_id (medicine_id) - 用于查询某药品的入库记录
- INDEX idx_in_date (in_date) - 用于按日期查询

#### 3.3.2.6 出库记录表（stock_out）

**表说明**：存储药品出库记录信息。

**表结构**：

| 列名 | 数据类型 | 长度 | 约束 | 默认值 | 说明 |
|------|---------|------|------|--------|------|
| id | BIGINT | - | PRIMARY KEY, AUTO_INCREMENT | - | 出库记录ID |
| medicine_id | BIGINT | - | NOT NULL, FOREIGN KEY | - | 药品ID，外键关联medicine.id |
| batch_number | VARCHAR | 50 | NULL | NULL | 批次号 |
| quantity | INT | - | NOT NULL | - | 出库数量 |
| out_date | DATE | - | NOT NULL | - | 出库日期 |
| operator | VARCHAR | 50 | NULL | NULL | 操作员 |
| reason | ENUM | - | NULL | NULL | 出库原因：'prescription'（处方发药）、'loss'（盘亏）、'expired'（过期）、'other'（其他） |
| create_time | TIMESTAMP | - | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |

**索引**：
- PRIMARY KEY (id)
- FOREIGN KEY fk_stock_out_medicine (medicine_id) REFERENCES medicine(id)
- INDEX idx_medicine_id (medicine_id) - 用于查询某药品的出库记录
- INDEX idx_out_date (out_date) - 用于按日期查询
- INDEX idx_reason (reason) - 用于按出库原因查询

#### 3.3.2.7 审核记录表（audit_record）

**表说明**：存储处方审核记录信息，包括自动审核和人工审核记录。

**表结构**：

| 列名 | 数据类型 | 长度 | 约束 | 默认值 | 说明 |
|------|---------|------|------|--------|------|
| id | BIGINT | - | PRIMARY KEY, AUTO_INCREMENT | - | 审核记录ID |
| prescription_id | BIGINT | - | NOT NULL, FOREIGN KEY | - | 处方ID，外键关联prescription.id |
| audit_type | ENUM | - | NOT NULL | 'auto' | 审核类型：'auto'（自动审核）、'manual'（人工审核） |
| audit_result | ENUM | - | NOT NULL | 'pass' | 审核结果：'pass'（通过）、'warning'（警告）、'reject'（拒绝） |
| audit_score | DECIMAL | 5,2 | NULL | NULL | 审核评分，0-100分 |
| issues_found | TEXT | - | NULL | NULL | 发现的问题 |
| suggestions | TEXT | - | NULL | NULL | 审核建议 |
| auditor | VARCHAR | 50 | NULL | NULL | 审核人 |
| audit_time | TIMESTAMP | - | NOT NULL | CURRENT_TIMESTAMP | 审核时间 |
| create_time | TIMESTAMP | - | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |

**索引**：
- PRIMARY KEY (id)
- FOREIGN KEY fk_audit_record_prescription (prescription_id) REFERENCES prescription(id)
- INDEX idx_prescription_id (prescription_id) - 用于查询某处方的审核历史
- INDEX idx_audit_type (audit_type) - 用于按审核类型查询
- INDEX idx_audit_result (audit_result) - 用于按审核结果查询
- INDEX idx_audit_time (audit_time) - 用于按审核时间查询

#### 3.3.2.8 外键约束说明

**外键关系**：

| 子表 | 外键列 | 父表 | 父表主键 | 级联操作 | 说明 |
|------|--------|------|---------|---------|------|
| prescription_detail | prescription_id | prescription | id | ON DELETE CASCADE | 删除处方时级联删除明细 |
| prescription_detail | medicine_id | medicine | id | ON DELETE RESTRICT | 删除药品前需检查是否有关联明细 |
| stock_in | medicine_id | medicine | id | ON DELETE RESTRICT | 删除药品前需检查是否有关联入库记录 |
| stock_out | medicine_id | medicine | id | ON DELETE RESTRICT | 删除药品前需检查是否有关联出库记录 |
| audit_record | prescription_id | prescription | id | ON DELETE CASCADE | 删除处方时级联删除审核记录 |

#### 3.3.2.9 数据库视图（可选）

当前系统未使用数据库视图，数据查询通过JPA Repository和Service层实现。如需提高查询性能，可考虑创建以下视图：

**（1）药品库存视图（v_medicine_stock）**
- 用途：汇总药品当前库存信息
- 包含字段：药品ID、药品名称、当前库存、最低库存、是否低库存

**（2）处方统计视图（v_prescription_statistics）**
- 用途：统计处方数量和状态分布
- 包含字段：日期、状态、数量

### 3.3.3 数据文件设计

系统支持通过Excel文件进行数据导入和导出，主要用于药品信息、库存记录的批量导入导出。

#### 3.3.3.1 药品信息导入导出文件结构

**文件格式**：Excel (.xlsx)

**文件结构**：

| 列名（A列） | 列名（B列） | 列名（C列） | 列名（D列） | 列名（E列） | 列名（F列） | 列名（G列） | 列名（H列） | 列名（I列） | 列名（J列） | 列名（K列） |
|-----------|-----------|-----------|-----------|-----------|-----------|-----------|-----------|-----------|-----------|-----------|
| 药品名称 | 通用名 | 规格 | 生产厂家 | 价格（元） | 库存数量 | 最低库存 | 药品分类 | 批准文号 | 适应症 | 禁忌症 |
| 阿莫西林胶囊 | 阿莫西林 | 0.25g*24粒 | 哈药集团 | 15.50 | 100 | 20 | 抗生素 | H20051234 | 用于敏感菌所致的各种感染 | 对青霉素类药物过敏者禁用 |

**字段说明**：

| 列名 | 数据类型 | 必填 | 长度限制 | 说明 |
|------|---------|------|---------|------|
| 药品名称 | 文本 | 是 | 最大100字符 | 药品的正式名称 |
| 通用名 | 文本 | 否 | 最大100字符 | 药品的通用名称 |
| 规格 | 文本 | 否 | 最大50字符 | 药品规格，如：0.25g*24粒 |
| 生产厂家 | 文本 | 否 | 最大100字符 | 生产厂家名称 |
| 价格（元） | 数字 | 是 | 小数2位 | 药品单价，单位：元 |
| 库存数量 | 整数 | 是 | - | 当前库存数量，必须≥0 |
| 最低库存 | 整数 | 否 | - | 最低库存预警值，默认10 |
| 药品分类 | 文本 | 否 | 最大50字符 | 药品分类，如：抗生素、解热镇痛等 |
| 批准文号 | 文本 | 否 | 最大50字符 | 药品批准文号 |
| 适应症 | 文本 | 否 | - | 药品适应症，支持多行文本 |
| 禁忌症 | 文本 | 否 | - | 药品禁忌症，支持多行文本 |
| 药物相互作用 | 文本 | 否 | - | 药物相互作用说明，支持多行文本 |

**导入规则**：
- 第一行为列标题，从第二行开始为数据
- 药品名称为必填字段，不能为空
- 价格为必填字段，必须≥0
- 库存数量为必填字段，必须≥0
- 如果药品名称已存在，根据导入模式选择：跳过、更新或报错

#### 3.3.3.2 库存记录导入导出文件结构

**文件格式**：Excel (.xlsx)

**（1）入库记录文件结构**

| 列名（A列） | 列名（B列） | 列名（C列） | 列名（D列） | 列名（E列） | 列名（F列） |
|-----------|-----------|-----------|-----------|-----------|-----------|
| 药品名称 | 批次号 | 入库数量 | 供应商 | 入库日期 | 操作员 |
| 阿莫西林胶囊 | BATCH20241201 | 100 | 医药公司A | 2024-12-01 | 张库管 |

**字段说明**：

| 列名 | 数据类型 | 必填 | 说明 |
|------|---------|------|------|
| 药品名称 | 文本 | 是 | 必须与系统中已存在的药品名称一致 |
| 批次号 | 文本 | 否 | 批次号，最大50字符 |
| 入库数量 | 整数 | 是 | 入库数量，必须>0 |
| 供应商 | 文本 | 否 | 供应商名称，最大100字符 |
| 入库日期 | 日期 | 是 | 格式：YYYY-MM-DD |
| 操作员 | 文本 | 否 | 操作员姓名，最大50字符 |

**（2）出库记录文件结构**

| 列名（A列） | 列名（B列） | 列名（C列） | 列名（D列） | 列名（E列） | 列名（F列） |
|-----------|-----------|-----------|-----------|-----------|-----------|
| 药品名称 | 批次号 | 出库数量 | 出库原因 | 出库日期 | 操作员 |
| 阿莫西林胶囊 | BATCH20241201 | 20 | 处方发药 | 2024-12-08 | 药房管理员 |

**字段说明**：

| 列名 | 数据类型 | 必填 | 说明 |
|------|---------|------|------|
| 药品名称 | 文本 | 是 | 必须与系统中已存在的药品名称一致 |
| 批次号 | 文本 | 否 | 批次号，最大50字符 |
| 出库数量 | 整数 | 是 | 出库数量，必须>0 |
| 出库原因 | 文本 | 否 | 出库原因：处方发药、盘亏、过期、其他 |
| 出库日期 | 日期 | 是 | 格式：YYYY-MM-DD |
| 操作员 | 文本 | 否 | 操作员姓名，最大50字符 |

#### 3.3.3.3 处方数据导出文件结构

**文件格式**：Excel (.xlsx)

**文件结构**：

| 列名（A列） | 列名（B列） | 列名（C列） | 列名（D列） | 列名（E列） | 列名（F列） | 列名（G列） | 列名（H列） | 列名（I列） | 列名（J列） | 列名（K列） |
|-----------|-----------|-----------|-----------|-----------|-----------|-----------|-----------|-----------|-----------|-----------|
| 处方编号 | 患者姓名 | 患者年龄 | 患者性别 | 诊断 | 医生姓名 | 科室 | 处方日期 | 处方状态 | 审核结果 | 审核时间 |
| RX20241209001 | 张三 | 35 | 男 | 上呼吸道感染 | 李医生 | 内科 | 2024-12-09 | 已发药 | 通过 | 2024-12-09 10:30:00 |

**说明**：处方数据仅支持导出，不支持导入（处方由医生在系统中录入）。

#### 3.3.3.4 数据文件导入导出规范

**（1）文件格式要求**：
- 文件格式：Excel 2007及以上版本（.xlsx）
- 文件编码：UTF-8
- 文件大小限制：最大10MB
- 数据行数限制：单次导入最多1000条记录

**（2）数据验证规则**：
- 必填字段验证：必填字段不能为空
- 数据类型验证：数字字段必须是数字，日期字段必须是有效日期格式
- 数据范围验证：数量必须>0，价格必须≥0
- 关联数据验证：药品名称必须在系统中存在

**（3）导入处理流程**：
1. 上传Excel文件
2. 解析Excel文件，读取数据
3. 逐行验证数据格式和完整性
4. 验证关联数据（如药品名称是否存在）
5. 批量保存数据
6. 返回导入结果（成功数量、失败数量、错误详情）

**（4）导出处理流程**：
1. 用户选择导出条件（如日期范围、状态等）
2. 查询符合条件的数据
3. 将数据转换为Excel格式
4. 生成Excel文件并提供下载链接

**（5）错误处理**：
- 导入过程中如果某行数据验证失败，记录错误信息，继续处理其他行
- 导入完成后返回详细的错误报告，包括失败行号、错误原因
- 支持部分成功导入，失败的记录不会影响已成功导入的数据

## 3.4 运行设计（可选项）

本系统是一个标准的Web应用系统，用户通过浏览器访问系统，系统运行时根据用户的操作请求动态加载相应的功能模块。由于系统功能相对集中，无需定义多个运行模式，所有功能在同一个运行环境中提供服务。

### 3.4.1 运行模块组合

**（1）前端模块组合**：
- **路由模块**：根据用户访问的URL路径，加载对应的Vue组件
- **状态管理模块**：管理全局状态（用户登录信息、权限信息等）
- **API通信模块**：处理前后端数据交互

**（2）后端模块组合**：
- **控制器模块**：接收HTTP请求，根据请求路径和方法调用对应的Service方法
- **业务逻辑模块**：执行具体的业务逻辑处理
- **数据访问模块**：通过JPA Repository访问数据库

**（3）运行流程**：
1. 用户通过浏览器访问系统
2. 前端路由模块加载对应的页面组件
3. 页面组件通过API通信模块向后端发送HTTP请求
4. 后端控制器接收请求，调用业务逻辑模块
5. 业务逻辑模块调用数据访问模块查询或更新数据
6. 数据访问模块通过JPA访问数据库
7. 结果逐层返回，最终通过HTTP响应返回给前端
8. 前端更新页面显示结果

### 3.4.2 运行控制

**（1）用户操作控制**：
- 用户通过点击按钮、输入表单等方式触发操作
- 前端进行输入验证，验证通过后发送请求
- 后端进行权限验证，验证通过后执行业务逻辑

**（2）模块调用控制**：
- Controller层控制：根据请求路径和方法，调用对应的Service方法
- Service层控制：根据业务逻辑，调用Repository或其他Service方法
- Repository层控制：根据查询条件，执行数据库操作

**（3）数据输入控制**：
- 前端表单验证：使用Element Plus的表单验证规则
- 后端参数验证：使用Bean Validation注解验证
- 数据类型转换：Spring自动进行类型转换，失败时返回400错误

### 3.4.3 运行时间

**（1）页面加载时间**：
- 首次访问：需要加载所有前端资源（JavaScript、CSS），预计2-3秒
- 后续访问：浏览器缓存资源，加载时间<1秒
- 数据加载：根据数据量，预计1-3秒

**（2）API响应时间**：
- 简单查询：<500毫秒
- 复杂查询：<3秒
- 数据操作：<2秒

**（3）数据库操作时间**：
- 单表查询：<100毫秒
- 关联查询：<500毫秒
- 数据更新：<200毫秒

**（4）性能优化措施**：
- 前端使用懒加载，按需加载组件
- 后端使用数据库索引优化查询性能
- 使用连接池管理数据库连接
- 合理使用分页，避免一次性加载大量数据

## 3.5 出错处理设计

### 3.5.1 错误信息

**（1）用户交互层错误信息**：

系统通过以下方式向用户展示错误信息：

- **消息提示框**：使用Element Plus的ElMessage组件，在页面右上角显示错误提示
  - 错误类型：error（红色）、warning（黄色）、info（蓝色）、success（绿色）
  - 显示时间：默认3秒后自动消失，用户可手动关闭
  - 显示内容：简洁明了的错误描述，必要时提供处理建议

- **表单验证错误**：在表单输入框下方显示红色错误提示文字
  - 实时验证：用户输入时实时显示验证结果
  - 提交验证：表单提交时统一显示所有验证错误

- **对话框提示**：对于重要操作（如删除），弹出确认对话框
  - 提示内容：操作描述和警告信息
  - 用户确认：用户点击确认后才执行操作

**常见错误信息示例**：
- "用户名或密码错误" - 登录失败
- "权限不足，需要管理员权限" - 权限验证失败
- "库存不足：当前库存为X，需要数量为Y" - 库存不足
- "处方明细不能为空" - 数据验证失败
- "网络错误，请检查网络连接" - 网络请求失败

**（2）后端日志记录错误信息**：

系统使用SLF4J + Logback记录错误日志，日志内容包括：

- **错误级别**：DEBUG、INFO、WARN、ERROR
- **日志内容**：
  - 错误发生时间
  - 错误级别和类型
  - 错误描述信息
  - 错误堆栈信息（ERROR级别）
  - 用户信息（如用户名、IP地址）
  - 请求信息（如请求路径、请求参数）

**日志文件位置**：
- 开发环境：控制台输出
- 生产环境：日志文件（logs/application.log）

**日志级别配置**：
- DEBUG：开发调试信息
- INFO：一般信息，如操作记录
- WARN：警告信息，如性能问题
- ERROR：错误信息，如异常、业务错误

### 3.5.2 补救措施

**（1）数据备份策略**：

**数据库备份**：
- **自动备份**：每天凌晨2:00自动备份数据库
- **备份方式**：使用mysqldump工具进行全量备份
- **备份存储**：备份文件存储在独立的备份服务器或云存储
- **备份保留**：保留最近30天的备份文件
- **备份验证**：定期验证备份文件的完整性

**备份脚本示例**：
```bash
# 每天凌晨2:00执行数据库备份
mysqldump -u root -p hpims > /backup/hpims_$(date +%Y%m%d).sql
```

**文件备份**：
- 系统配置文件：版本控制（Git）
- 上传的文件：定期备份到独立存储

**（2）数据恢复机制**：

**数据库恢复**：
- **恢复方式**：使用备份的SQL文件恢复数据库
- **恢复步骤**：
  1. 停止应用程序
  2. 备份当前数据库（以防万一）
  3. 使用mysqldump备份文件恢复数据库
  4. 验证数据完整性
  5. 重启应用程序

**事务回滚**：
- 使用Spring的`@Transactional`注解管理事务
- 关键操作（处方创建、审核、发药）使用事务保证数据一致性
- 操作失败时自动回滚，保证数据完整性

**（3）异常恢复措施**：

**数据库连接异常**：
- 使用连接池管理数据库连接
- 连接断开时自动重连
- 连接池配置最大重试次数

**Python审核服务异常**：
- 调用超时：设置5秒超时时间，超时后记录日志，等待人工审核
- 服务不可用：记录错误日志，不影响处方创建，等待人工审核

**网络异常**：
- 前端请求失败：显示网络错误提示，用户可重试
- 后端请求失败：记录错误日志，返回友好的错误提示

**（4）数据一致性检查**：

**库存一致性检查**：
- 定期检查药品库存数量与实际入库、出库记录是否一致
- 发现不一致时记录警告日志，管理员可手动修正

**处方状态一致性检查**：
- 检查处方状态与审核记录是否一致
- 发现不一致时记录警告日志

**（5）监控和告警**：

**系统监控**：
- 监控系统运行状态（CPU、内存、磁盘使用率）
- 监控数据库连接数、查询性能
- 监控API响应时间

**告警机制**：
- 系统资源使用率超过阈值时发送告警
- 数据库连接数过多时发送告警
- API响应时间过长时发送告警
- 错误日志达到阈值时发送告警