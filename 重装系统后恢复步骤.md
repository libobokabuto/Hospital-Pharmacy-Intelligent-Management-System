# 重装系统后项目恢复步骤清单

## 📋 准备工作

### 1. 安装必要的开发环境

按顺序安装以下软件：

#### ✅ JDK 17+
- 下载地址：https://adoptium.net/ 或 Oracle官网
- 安装后配置环境变量：
  - `JAVA_HOME`: JDK安装路径（如 `C:\Program Files\Java\jdk-17`）
  - `Path`: 添加 `%JAVA_HOME%\bin`
- 验证：`java -version`

#### ✅ MySQL 8.0
- 下载地址：https://dev.mysql.com/downloads/mysql/
- 安装时设置root密码：**20050129**（与配置文件一致）
- 启动MySQL服务（Windows服务管理器）
- 验证：`mysql -u root -p`

#### ✅ Maven 3.6+
- 下载地址：https://maven.apache.org/download.cgi
- 解压到 `C:\Program Files\maven`
- 配置环境变量：
  - `MAVEN_HOME`: Maven解压路径
  - `Path`: 添加 `%MAVEN_HOME%\bin`
- 验证：`mvn -version`

#### ✅ Python 3.8+
- 下载地址：https://www.python.org/downloads/
- 安装时勾选 "Add Python to PATH"
- 验证：`python --version` 和 `pip --version`

#### ✅ Node.js 16+
- 下载地址：https://nodejs.org/（选择LTS版本）
- 验证：`node --version` 和 `npm --version`

---

## 🚀 项目恢复步骤

### 第1步：克隆项目

```bash
# 进入你的工作目录（如 F:\HPIMS）
cd F:\HPIMS

# 克隆项目（使用你的Git仓库地址）
git clone <你的项目Git地址>
# 或者如果已经有项目，直接进入目录
cd Hospital-Pharmacy-Intelligent-Management-System
```

### 第2步：配置数据库

#### 2.1 启动MySQL服务
- Windows：打开"服务"管理器，启动"MySQL80"服务

#### 2.2 创建数据库
```bash
# 登录MySQL
mysql -u root -p
# 输入密码：20050129
```

```sql
-- 创建数据库
CREATE DATABASE hpims CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 使用数据库
USE hpims;

-- 执行初始化脚本（注意路径）
SOURCE F:/HPIMS/Hospital-Pharmacy-Intelligent-Management-System/database/init.sql;

-- 验证表是否创建成功
SHOW TABLES;
SELECT * FROM sys_user LIMIT 1;
```

#### 2.3 验证数据库配置
检查 `backend/src/main/resources/application.yml` 中的数据库配置：
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hpims?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&zeroDateTimeBehavior=convertToNull
    username: root
    password: 20050129  # 确保密码正确
```

### 第3步：安装项目依赖

#### 3.1 安装后端依赖（Java）
```bash
cd backend
mvn clean install
```
**等待Maven下载所有依赖包（可能需要几分钟）**

#### 3.2 安装前端依赖（Node.js）
```bash
cd ../frontend
npm install
```
**等待npm安装所有依赖包（可能需要几分钟）**

#### 3.3 安装Python依赖
```bash
cd ../python-audit-service
pip install -r requirements.txt
```

---

## 🎯 启动项目

### 方式1：分别启动三个服务（推荐用于开发）

#### 启动后端服务（第1个终端窗口）
```bash
cd backend
mvn spring-boot:run
```
**成功标志**：看到 `Started HpimsApplication` 和端口 `8080`

#### 启动Python审核服务（第2个终端窗口）
```bash
cd python-audit-service
python src/app.py
```
**成功标志**：看到 `Running on http://localhost:5000`

#### 启动前端服务（第3个终端窗口）
```bash
cd frontend
npm run dev
```
**成功标志**：看到 `Local: http://localhost:5173` 或 `http://localhost:3000`

### 方式2：使用启动脚本（如果有）
```bash
# Windows
start.bat

# Linux/Mac
chmod +x start.sh
./start.sh
```

---

## ✅ 验证服务是否正常

### 1. 检查后端服务
打开浏览器访问：http://localhost:8080/api/swagger-ui.html
- 能看到Swagger API文档说明后端正常

### 2. 检查Python服务
打开浏览器访问：http://localhost:5000/health
- 应该返回：`{"status": "healthy"}`

### 3. 检查前端服务
打开浏览器访问：http://localhost:5173 或 http://localhost:3000
- 应该看到登录页面

### 4. 测试登录
使用默认账号登录：
- **管理员**：admin / 123456
- **医生**：doctor / 123456
- **药师**：pharmacist / 123456
- **护士**：nurse / 123456

---

## 🔧 常见问题排查

### ❌ 问题1：Java版本不对
```bash
java -version
# 确保是JDK 17或更高版本
```

### ❌ 问题2：MySQL连接失败
1. 检查MySQL服务是否启动
2. 验证密码是否正确（默认：20050129）
3. 检查 `application.yml` 中的数据库配置

### ❌ 问题3：端口被占用
```bash
# Windows检查端口占用
netstat -ano | findstr :8080
netstat -ano | findstr :5173
netstat -ano | findstr :5000

# 杀死进程
taskkill /PID <进程ID> /F
```

### ❌ 问题4：Maven依赖下载失败
```bash
# 清理并重新安装
cd backend
mvn clean install -U
```

### ❌ 问题5：npm安装失败
```bash
# 删除node_modules重新安装
cd frontend
rmdir /s /q node_modules
npm install
```

### ❌ 问题6：Python依赖安装失败
```bash
# 使用国内镜像源
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

---

## 📝 重要配置文件位置

- **后端数据库配置**：`backend/src/main/resources/application.yml`
- **前端API配置**：`frontend/src/api/index.js`
- **Python服务配置**：`python-audit-service/config/settings.py`
- **数据库初始化脚本**：`database/init.sql`

---

## 🎉 完成！

如果所有服务都正常启动，你就可以开始开发了！

**访问地址**：
- 前端：http://localhost:5173
- 后端API：http://localhost:8080/api
- API文档：http://localhost:8080/api/swagger-ui.html
- Python服务：http://localhost:5000

**默认账号**：admin / 123456

---

## 💡 提示

1. **保存好数据库密码**：默认是 `20050129`，如果修改了记得同步更新配置文件
2. **三个服务都要启动**：后端、前端、Python服务缺一不可
3. **开发时保持服务运行**：修改代码后前端会自动刷新，后端需要重启
4. **遇到问题先看日志**：控制台的错误信息通常能帮你定位问题

祝你顺利恢复项目！🚀

