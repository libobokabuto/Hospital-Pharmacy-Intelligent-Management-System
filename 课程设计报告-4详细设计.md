# 4 详细设计

本章节对系统的各个功能模块进行详细设计说明，包括模块功能描述、接口设计、用户界面设计、程序结构设计、算法流程等。系统共分为6个主要功能模块，每个模块都有其独立的职责和设计考虑。

## 4.1 用户认证与权限管理模块设计说明

**模块标识符**：M-AUTH

### 4.1.1 模块功能描述

用户认证与权限管理模块是系统的安全基础模块，负责用户的身份认证、权限控制和用户信息管理。本模块采用JWT（JSON Web Token）无状态认证机制，结合Spring Security框架实现基于角色的访问控制（RBAC）。

**主要功能**：
1. **用户登录认证**：验证用户身份，生成JWT令牌
2. **用户注册**：支持新用户注册（管理员创建用户）
3. **令牌管理**：JWT令牌的生成、验证、刷新
4. **用户信息管理**：用户信息的查询、更新、删除
5. **角色权限管理**：基于角色的权限控制（admin、doctor、nurse、pharmacist）

**模块特点**：
- 采用无状态JWT认证，支持分布式部署
- 密码使用BCrypt加密存储，安全性高
- 支持令牌刷新机制，提升用户体验
- 权限控制细粒度，支持方法级权限验证

### 4.1.2 模块性能要求

1. **响应时间要求**：
   - 用户登录响应时间 ≤ 500ms
   - 用户信息查询响应时间 ≤ 200ms
   - 令牌验证响应时间 ≤ 100ms

2. **并发性能要求**：
   - 支持至少100个并发登录请求
   - JWT令牌验证不依赖数据库查询，性能优异

3. **安全性要求**：
   - JWT令牌有效期：24小时
   - 令牌刷新有效期：7天
   - 密码加密强度：BCrypt，cost factor = 10

### 4.1.3 模块接口设计

#### 4.1.3.1 认证接口（AuthController）

**（1）用户登录接口**
- **接口路径**：`POST /api/auth/login`
- **请求参数**：
  ```json
  {
    "username": "string",  // 用户名，必填
    "password": "string"   // 密码，必填
  }
  ```
- **响应数据**：
  ```json
  {
    "code": 200,
    "message": "登录成功",
    "data": {
      "token": "string",      // JWT令牌
      "username": "string",   // 用户名
      "role": "string",       // 角色（admin/doctor/nurse/pharmacist）
      "realName": "string",   // 真实姓名
      "userId": "long"        // 用户ID
    }
  }
  ```
- **错误响应**：
  - 401：用户名或密码错误
  - 400：请求参数验证失败

**（2）用户注册接口**
- **接口路径**：`POST /api/auth/register`
- **请求参数**：
  ```json
  {
    "username": "string",      // 用户名，必填，唯一
    "password": "string",      // 密码，必填，长度6-20
    "role": "string",          // 角色，必填
    "realName": "string",      // 真实姓名，必填
    "department": "string",    // 科室，可选
    "employeeNumber": "string", // 职工号，可选
    "title": "string"          // 职称，可选
  }
  ```
- **响应数据**：用户信息对象
- **错误响应**：
  - 400：用户名已存在或参数验证失败

**（3）获取当前用户信息接口**
- **接口路径**：`GET /api/auth/me`
- **请求头**：`Authorization: Bearer {token}`
- **响应数据**：当前登录用户信息
- **错误响应**：
  - 401：未认证或令牌无效
  - 404：用户不存在

**（4）刷新令牌接口**
- **接口路径**：`POST /api/auth/refresh`
- **请求参数**：
  ```json
  {
    "token": "string"  // 旧JWT令牌
  }
  ```
- **响应数据**：新的登录响应（包含新令牌）
- **错误响应**：
  - 401：令牌无效或已过期

**（5）用户登出接口**
- **接口路径**：`POST /api/auth/logout`
- **请求头**：`Authorization: Bearer {token}`
- **响应数据**：成功消息
- **说明**：JWT无状态，客户端删除token即可，此接口用于清空服务端上下文

#### 4.1.3.2 用户管理接口（UserController）

**（1）获取用户列表接口**
- **接口路径**：`GET /api/users`
- **请求参数**：
  - `page`: 页码，默认0
  - `size`: 每页大小，默认10
  - `keyword`: 搜索关键词（用户名/真实姓名），可选
- **权限要求**：管理员
- **响应数据**：分页用户列表

**（2）创建用户接口**
- **接口路径**：`POST /api/users`
- **请求参数**：同注册接口
- **权限要求**：管理员
- **响应数据**：创建的用户信息

**（3）获取用户详情接口**
- **接口路径**：`GET /api/users/{id}`
- **权限要求**：所有认证用户
- **响应数据**：用户详细信息

**（4）更新用户信息接口**
- **接口路径**：`PUT /api/users/{id}`
- **请求参数**：用户信息对象（部分字段可更新）
- **权限要求**：管理员或本人
- **响应数据**：更新后的用户信息

**（5）删除用户接口**
- **接口路径**：`DELETE /api/users/{id}`
- **权限要求**：管理员
- **响应数据**：成功消息

**（6）修改用户角色接口**
- **接口路径**：`PUT /api/users/{id}/role`
- **请求参数**：
  ```json
  {
    "role": "string"  // 新角色
  }
  ```
- **权限要求**：管理员
- **响应数据**：更新后的用户信息

#### 4.1.3.3 数据传递对象（DTO）

**请求DTO**：
- `LoginRequest`：登录请求，包含username和password
- `RegisterRequest`：注册请求，包含用户完整信息
- `RefreshTokenRequest`：刷新令牌请求，包含旧token

**响应DTO**：
- `LoginResponse`：登录响应，包含token和用户基本信息
- `UserResponse`：用户信息响应，包含用户详细信息（不包含密码）

#### 4.1.3.4 关联数据结构

**数据库表**：
- `users`表：存储用户基本信息
  - 主键：`id`
  - 唯一索引：`username`
  - 字段：username, password, role, real_name, department, employee_number, title等

**相关模块调用**：
- 调用`UserService`进行用户业务逻辑处理
- 调用`JwtUtil`进行JWT令牌生成和验证
- 调用`PasswordEncoder`进行密码加密和验证
- 通过Spring Security进行权限验证

### 4.1.4 用户界面设计

#### 4.1.4.1 登录界面

**界面布局**：
- 居中显示登录表单
- 包含用户名输入框、密码输入框、登录按钮
- 显示系统标题和Logo

**操作步骤**：
1. 用户输入用户名和密码
2. 点击"登录"按钮
3. 系统验证用户身份
4. 登录成功后跳转到对应角色的首页
5. 登录失败显示错误提示

**注意事项**：
- 密码输入框使用密码类型（隐藏显示）
- 登录按钮在请求处理期间显示加载状态
- 错误信息清晰提示（用户名或密码错误，不暴露具体是用户名还是密码错误）

#### 4.1.4.2 用户管理界面（管理员）

**界面布局**：
- 顶部：搜索框和"新增用户"按钮
- 中间：用户列表表格
- 表格列：ID、用户名、真实姓名、角色、科室、职工号、职称、操作（编辑/删除）

**操作步骤**：

**新增用户**：
1. 点击"新增用户"按钮，弹出表单对话框
2. 填写用户信息（用户名、密码、角色、真实姓名等）
3. 点击"确定"保存，点击"取消"关闭对话框
4. 保存成功后刷新列表

**编辑用户**：
1. 点击用户行的"编辑"按钮，弹出表单对话框（预填充用户信息）
2. 修改用户信息
3. 点击"确定"保存，点击"取消"关闭对话框
4. 保存成功后刷新列表

**删除用户**：
1. 点击用户行的"删除"按钮
2. 弹出确认对话框："确定要删除该用户吗？此操作不可恢复！"
3. 点击"确定"执行删除，点击"取消"取消操作
4. 删除成功后刷新列表

**搜索用户**：
1. 在搜索框输入关键词（用户名或真实姓名）
2. 自动触发搜索（或点击搜索按钮）
3. 显示匹配的用户列表

**注意事项**：
- 用户名、密码、角色、真实姓名为必填项
- 删除用户前需要二次确认
- 操作成功后显示成功提示，失败显示错误提示
- 列表支持分页

### 4.1.5 程序结构设计

#### 4.1.5.1 设计类图

**核心类**：

```
AuthController
├── AuthenticationManager (Spring Security)
├── UserService
├── JwtUtil
└── PasswordEncoder

UserController
├── UserService
├── PasswordEncoder
└── SecurityContextHolder

UserService
├── UserRepository
└── PasswordEncoder

JwtUtil
├── SecretKey (JWT签名密钥)
└── JWT Library (io.jsonwebtoken)

JwtAuthenticationFilter
├── JwtUtil
├── UserDetailsService
└── SecurityContextHolder

SecurityConfig
├── JwtAuthenticationFilter
├── PasswordEncoder
└── AuthenticationManager
```

**类职责说明**：

- **AuthController**：处理认证相关HTTP请求（登录、注册、刷新令牌等）
- **UserController**：处理用户管理相关HTTP请求（CRUD操作）
- **UserService**：用户业务逻辑处理（用户查询、创建、更新、删除）
- **JwtUtil**：JWT令牌工具类（生成、解析、验证令牌）
- **JwtAuthenticationFilter**：JWT认证过滤器（拦截请求，验证令牌）
- **SecurityConfig**：Spring Security配置类（配置安全策略、过滤器链）

#### 4.1.5.2 数据结构描述

**User实体类**：
```java
public class User {
    private Long id;                    // 用户ID（主键）
    private String username;            // 用户名（唯一）
    private String password;            // 密码（BCrypt加密）
    private String role;                // 角色（admin/doctor/nurse/pharmacist）
    private String realName;            // 真实姓名
    private String department;          // 科室
    private String employeeNumber;      // 职工号
    private String title;               // 职称
    private LocalDateTime createTime;   // 创建时间
    private LocalDateTime updateTime;   // 更新时间
}
```

**JWT Payload结构**：
```json
{
  "sub": "username",           // 主题（用户名）
  "role": "doctor",            // 角色
  "iat": 1234567890,          // 签发时间（Unix时间戳）
  "exp": 1234654290           // 过期时间（Unix时间戳）
}
```

### 4.1.6 算法及流程逻辑

#### 4.1.6.1 用户登录流程

**流程图描述**：

```
开始
  ↓
接收登录请求（username, password）
  ↓
查找用户（根据username）
  ↓
用户是否存在？
  ├─ 否 → 返回"用户名或密码错误" → 结束
  ↓ 是
使用PasswordEncoder验证密码
  ↓
密码是否正确？
  ├─ 否 → 返回"用户名或密码错误" → 结束
  ↓ 是
创建Authentication对象
  ↓
设置到SecurityContext
  ↓
生成JWT令牌（包含username和role）
  ↓
构建LoginResponse（包含token和用户信息）
  ↓
返回成功响应
  ↓
结束
```

**关键算法**：

1. **密码验证算法**：
   ```
   使用BCryptPasswordEncoder.matches(明文密码, 哈希密码)
   返回true/false
   ```

2. **JWT令牌生成算法**：
   ```
   设置Claims：
     - sub = username
     - role = user.getRole()
     - iat = 当前时间
     - exp = 当前时间 + 24小时
   使用HS256算法和密钥签名
   返回Base64编码的令牌字符串
   ```

#### 4.1.6.2 JWT令牌验证流程

**流程图描述**：

```
开始
  ↓
从HTTP请求头提取token（Authorization: Bearer {token}）
  ↓
token是否存在？
  ├─ 否 → 放行（如果是公开路径）或返回401 → 结束
  ↓ 是
解析token（提取username和role）
  ↓
验证token签名和过期时间
  ↓
token是否有效？
  ├─ 否 → 返回401 → 结束
  ↓ 是
从数据库加载用户信息（根据username）
  ↓
用户是否存在？
  ├─ 否 → 返回401 → 结束
  ↓ 是
创建Authentication对象
  ↓
设置到SecurityContext
  ↓
继续处理请求
  ↓
结束
```

#### 4.1.6.3 用户注册流程

**流程图描述**：

```
开始
  ↓
接收注册请求
  ↓
验证请求参数（@Valid）
  ↓
检查用户名是否已存在
  ↓
用户名是否已存在？
  ├─ 是 → 返回"用户名已存在" → 结束
  ↓ 否
使用PasswordEncoder加密密码
  ↓
创建User对象
  ↓
设置创建时间和更新时间
  ↓
保存用户到数据库
  ↓
构建UserResponse（不包含密码）
  ↓
返回成功响应
  ↓
结束
```

#### 4.1.6.4 权限验证流程

**流程图描述**：

```
开始
  ↓
请求到达Controller方法
  ↓
提取当前用户（从SecurityContext）
  ↓
检查用户角色
  ↓
角色是否满足要求？
  ├─ 否 → 返回403 Forbidden → 结束
  ↓ 是
执行Controller方法逻辑
  ↓
返回响应
  ↓
结束
```

**权限验证示例**（伪代码）：
```java
// 检查是否为管理员
private boolean isAdmin() {
    Authentication auth = SecurityContextHolder.getContext().getAuthentication();
    return auth != null && auth.getAuthorities().stream()
        .anyMatch(a -> a.getAuthority().equals("ROLE_admin"));
}

// 检查是否为医生或管理员
private boolean isDoctorOrAdmin() {
    Authentication auth = SecurityContextHolder.getContext().getAuthentication();
    if (auth == null) return false;
    return auth.getAuthorities().stream()
        .anyMatch(a -> a.getAuthority().equals("ROLE_doctor") 
                   || a.getAuthority().equals("ROLE_admin"));
}
```

### 4.1.7 其它说明

1. **安全性考虑**：
   - JWT密钥应存储在配置文件中，生产环境应使用环境变量或密钥管理服务
   - 密码使用BCrypt加密，cost factor建议设置为10-12
   - 敏感操作（如删除用户、修改角色）需要管理员权限
   - 登录失败不暴露具体是用户名还是密码错误，防止用户名枚举攻击

2. **性能优化**：
   - JWT令牌验证不依赖数据库查询，性能优异
   - 用户信息查询使用缓存（可选的优化方案）
   - 密码加密使用BCrypt，虽然计算成本较高，但安全性更好

3. **扩展性考虑**：
   - 支持令牌刷新机制，提升用户体验
   - 可以扩展支持多种认证方式（如短信验证码、第三方登录等）
   - 权限系统可以扩展为更细粒度的权限控制（基于资源的权限控制）

4. **错误处理**：
   - 统一的错误响应格式（使用ApiResponse）
   - 详细的错误日志记录
   - 用户友好的错误提示信息

5. **测试建议**：
   - 单元测试：测试密码加密和验证、JWT生成和验证
   - 集成测试：测试登录、注册、权限验证等完整流程
   - 安全测试：测试SQL注入、XSS攻击等安全漏洞

---

## 4.2 药品管理模块设计说明

**模块标识符**：M-MED

### 4.2.1 模块功能描述

药品管理模块负责医院药房药品信息的管理，包括药品的基本信息维护、查询、分类管理和低库存预警等功能。本模块是系统的核心基础模块，为处方管理、库存管理等模块提供药品基础数据支持。

**主要功能**：
1. **药品信息管理**：药品的增删改查（CRUD）操作
2. **药品搜索**：支持按药品名称、规格、生产厂家等条件搜索
3. **分类管理**：支持按药品分类查询（如：西药、中药、中成药等）
4. **低库存预警**：自动识别库存低于安全库存的药品
5. **药品详情查看**：查看药品的详细信息，包括适应症、禁忌症、药物相互作用等

**模块特点**：
- 药品信息字段完整，包括基本信息、价格信息、存储信息、临床信息等
- 支持药品的软删除，删除的药品仍可查询历史记录
- 提供灵活的搜索和筛选功能
- 与库存管理模块紧密集成，实时获取库存信息

### 4.2.2 模块性能要求

1. **响应时间要求**：
   - 药品列表查询响应时间 ≤ 500ms（1000条数据以内）
   - 药品搜索响应时间 ≤ 300ms
   - 药品详情查询响应时间 ≤ 200ms

2. **数据量要求**：
   - 支持至少10000条药品记录
   - 支持分页查询，每页最多100条

3. **并发性能要求**：
   - 支持至少50个并发查询请求

### 4.2.3 模块接口设计

#### 4.2.3.1 药品管理接口（MedicineController）

**（1）获取药品列表接口**
- **接口路径**：`GET /api/medicines`
- **请求参数**：
  - `page`: 页码，默认0
  - `size`: 每页大小，默认10，最大100
  - `keyword`: 搜索关键词（药品名称/规格/生产厂家），可选
  - `category`: 药品分类，可选
- **权限要求**：所有认证用户
- **响应数据**：分页药品列表

**（2）获取药品详情接口**
- **接口路径**：`GET /api/medicines/{id}`
- **权限要求**：所有认证用户
- **响应数据**：药品详细信息

**（3）创建药品接口**
- **接口路径**：`POST /api/medicines`
- **请求参数**：
  ```json
  {
    "name": "string",              // 药品名称，必填
    "specification": "string",     // 规格，必填
    "unit": "string",              // 单位，必填
    "category": "string",          // 分类，必填
    "manufacturer": "string",      // 生产厂家，可选
    "price": "decimal",            // 单价，必填，≥0
    "stockQuantity": "integer",    // 库存数量，默认0，≥0
    "safetyStock": "integer",      // 安全库存，默认0，≥0
    "expiryDays": "integer",       // 有效期天数，可选
    "indication": "string",        // 适应症，可选
    "contraindication": "string",  // 禁忌症，可选
    "interactions": "string"       // 药物相互作用，可选
  }
  ```
- **权限要求**：管理员或药师
- **响应数据**：创建的药品信息

**（4）更新药品信息接口**
- **接口路径**：`PUT /api/medicines/{id}`
- **请求参数**：同创建接口（部分字段可更新）
- **权限要求**：管理员或药师
- **响应数据**：更新后的药品信息

**（5）删除药品接口**
- **接口路径**：`DELETE /api/medicines/{id}`
- **权限要求**：管理员
- **响应数据**：成功消息
- **说明**：软删除，仅标记删除状态，不实际删除数据

**（6）搜索药品接口**
- **接口路径**：`GET /api/medicines/search`
- **请求参数**：
  - `keyword`: 搜索关键词，必填
  - `page`: 页码，默认0
  - `size`: 每页大小，默认10
- **权限要求**：所有认证用户
- **响应数据**：分页药品列表（匹配关键词的药品）

**（7）按分类查询药品接口**
- **接口路径**：`GET /api/medicines/category/{category}`
- **请求参数**：
  - `category`: 药品分类，必填（路径参数）
  - `page`: 页码，默认0
  - `size`: 每页大小，默认10
- **权限要求**：所有认证用户
- **响应数据**：分页药品列表（指定分类的药品）

**（8）查询低库存药品接口**
- **接口路径**：`GET /api/medicines/low-stock`
- **请求参数**：
  - `page`: 页码，默认0
  - `size`: 每页大小，默认10
- **权限要求**：所有认证用户
- **响应数据**：分页药品列表（库存低于安全库存的药品）
- **筛选条件**：`stockQuantity < safetyStock`

#### 4.2.3.2 数据传递对象（DTO）

**请求DTO**：
- `MedicineCreateRequest`：创建药品请求，包含药品完整信息
- `MedicineUpdateRequest`：更新药品请求，包含可更新的字段

**响应DTO**：
- `MedicineResponse`：药品信息响应，包含药品详细信息

#### 4.2.3.3 关联数据结构

**数据库表**：
- `medicine`表：存储药品基本信息
  - 主键：`id`
  - 字段：name, specification, unit, category, manufacturer, price, stock_quantity, safety_stock, expiry_days, indication, contraindication, interactions等
  - 索引：name（用于搜索优化）

**相关模块调用**：
- 调用`MedicineService`进行药品业务逻辑处理
- 调用`MedicineRepository`进行数据持久化
- 被`StockService`调用，用于更新药品库存
- 被`PrescriptionService`调用，用于验证药品存在性和获取药品信息

### 4.2.4 用户界面设计

#### 4.2.4.1 药品管理界面（管理员/药师）

**界面布局**：
- 顶部：搜索框、筛选条件（分类筛选）、"新增药品"按钮
- 中间：药品列表表格
- 表格列：ID、药品名称、规格、单位、分类、生产厂家、单价、库存数量、安全库存、操作（查看/编辑/删除）

**操作步骤**：

**新增药品**：
1. 点击"新增药品"按钮，弹出表单对话框
2. 填写药品信息：
   - 基本信息：药品名称、规格、单位、分类
   - 价格信息：单价
   - 库存信息：初始库存数量、安全库存
   - 临床信息：适应症、禁忌症、药物相互作用
3. 点击"确定"保存，点击"取消"关闭对话框
4. 保存成功后刷新列表

**编辑药品**：
1. 点击药品行的"编辑"按钮，弹出表单对话框（预填充药品信息）
2. 修改药品信息（注意：库存数量不能直接修改，需通过入库/出库操作）
3. 点击"确定"保存，点击"取消"关闭对话框
4. 保存成功后刷新列表

**删除药品**：
1. 点击药品行的"删除"按钮
2. 弹出确认对话框："确定要删除该药品吗？此操作不可恢复！"
3. 点击"确定"执行删除，点击"取消"取消操作
4. 删除成功后刷新列表

**搜索药品**：
1. 在搜索框输入关键词（药品名称、规格、生产厂家）
2. 自动触发搜索（或点击搜索按钮）
3. 显示匹配的药品列表

**分类筛选**：
1. 选择药品分类下拉框（如：西药、中药、中成药等）
2. 自动筛选显示指定分类的药品

**查看低库存药品**：
1. 点击"低库存预警"按钮或标签
2. 筛选显示库存数量低于安全库存的药品
3. 低库存药品以红色或警告颜色标识

**注意事项**：
- 药品名称、规格、单位、分类、单价为必填项
- 单价必须≥0
- 库存数量、安全库存必须≥0
- 删除药品前需要二次确认
- 操作成功后显示成功提示，失败显示错误提示
- 列表支持分页
- 库存数量通过入库/出库操作自动更新，不应在药品编辑中直接修改

### 4.2.5 程序结构设计

#### 4.2.5.1 设计类图

**核心类**：

```
MedicineController
├── MedicineService
└── SecurityContextHolder (权限验证)

MedicineService
├── MedicineRepository
└── MedicineNotFoundException (异常处理)

MedicineRepository
└── JpaRepository<Medicine, Long> (继承Spring Data JPA)
```

**类职责说明**：

- **MedicineController**：处理药品管理相关HTTP请求（CRUD操作、搜索、分类查询、低库存查询）
- **MedicineService**：药品业务逻辑处理（药品查询、创建、更新、删除、搜索、库存更新）
- **MedicineRepository**：药品数据访问层（继承JPA Repository，提供标准CRUD和自定义查询方法）

#### 4.2.5.2 数据结构描述

**Medicine实体类**：
```java
public class Medicine {
    private Long id;                    // 药品ID（主键）
    private String name;                // 药品名称
    private String genericName;         // 通用名
    private String specification;       // 规格
    private String unit;                // 单位
    private String category;            // 分类
    private String manufacturer;        // 生产厂家
    private BigDecimal price;           // 单价
    private Integer stockQuantity;      // 库存数量
    private Integer safetyStock;        // 安全库存
    private Integer expiryDays;         // 有效期天数
    private String indication;          // 适应症
    private String contraindication;    // 禁忌症
    private String interactions;        // 药物相互作用
    private String approvalNumber;      // 批准文号
    private LocalDateTime createTime;   // 创建时间
    private LocalDateTime updateTime;   // 更新时间
}
```

### 4.2.6 算法及流程逻辑

#### 4.2.6.1 药品创建流程

**流程图描述**：

```
开始
  ↓
接收创建药品请求
  ↓
验证请求参数（@Valid）
  ↓
检查必填字段
  ↓
字段是否完整？
  ├─ 否 → 返回"参数验证失败" → 结束
  ↓ 是
创建Medicine对象
  ↓
设置创建时间和更新时间
  ↓
保存药品到数据库
  ↓
返回创建的药品信息
  ↓
结束
```

#### 4.2.6.2 药品更新流程

**流程图描述**：

```
开始
  ↓
接收更新药品请求（包含药品ID）
  ↓
根据ID查询药品
  ↓
药品是否存在？
  ├─ 否 → 抛出MedicineNotFoundException → 返回404 → 结束
  ↓ 是
更新药品字段（不更新库存数量）
  ↓
设置更新时间
  ↓
保存药品到数据库
  ↓
返回更新后的药品信息
  ↓
结束
```

#### 4.2.6.3 药品搜索流程

**流程图描述**：

```
开始
  ↓
接收搜索请求（包含关键词）
  ↓
关键词是否为空？
  ├─ 是 → 返回所有药品 → 结束
  ↓ 否
使用MedicineRepository.findByNameContaining(keyword)查询
  ↓
返回匹配的药品列表
  ↓
结束
```

**关键算法**：

1. **模糊搜索算法**：
   ```
   使用JPA的Containing查询方法：
   findByNameContaining(String keyword)
   等价于SQL: WHERE name LIKE '%keyword%'
   支持药品名称的模糊匹配
   ```

2. **低库存筛选算法**：
   ```
   查询条件：stockQuantity < safetyStock
   使用JPA查询方法：
   findByStockQuantityLessThan(Integer safetyStock)
   或使用@Query注解自定义查询
   ```

#### 4.2.6.4 分类查询流程

**流程图描述**：

```
开始
  ↓
接收分类查询请求（包含分类名称）
  ↓
使用MedicineRepository.findByCategory(category)查询
  ↓
返回指定分类的药品列表
  ↓
结束
```

### 4.2.7 其它说明

1. **数据完整性**：
   - 药品信息字段完整，包括基本信息、价格信息、临床信息
   - 使用数据库约束（NOT NULL、CHECK等）保证数据完整性
   - 价格、库存数量等数值字段使用合适的精度和范围

2. **性能优化**：
   - 药品名称字段建立索引，提升搜索性能
   - 使用分页查询，避免一次性加载大量数据
   - 搜索操作使用数据库索引优化

3. **业务规则**：
   - 库存数量不能通过药品更新接口直接修改，必须通过入库/出库操作
   - 删除药品时需要考虑是否有关联的处方记录（可考虑软删除）
   - 低库存预警基于库存数量与安全库存的比较

4. **扩展性考虑**：
   - 支持药品图片上传（可选的扩展功能）
   - 支持药品批号管理（可选的扩展功能）
   - 支持药品有效期管理（可选的扩展功能）

5. **错误处理**：
   - 使用MedicineNotFoundException处理药品不存在的情况
   - 使用参数验证注解（@NotNull、@DecimalMin等）保证参数有效性
   - 统一的错误响应格式（使用ApiResponse）

---

## 4.3 库存管理模块设计说明

**模块标识符**：M-STOCK

### 4.3.1 模块功能描述

库存管理模块负责医院药房药品的入库、出库操作和库存统计查询。本模块与药品管理模块紧密集成，实现药品库存的自动更新和管理。库存管理是药房管理的核心环节，确保药品库存数据的准确性和实时性。

**主要功能**：
1. **药品入库管理**：记录药品入库信息，自动更新药品库存数量
2. **药品出库管理**：记录药品出库信息，自动扣减药品库存数量，支持多种出库原因（处方发药、盘亏、过期、其他）
3. **入库记录查询**：支持按药品ID、日期范围等条件查询入库记录
4. **出库记录查询**：支持按药品ID、日期范围等条件查询出库记录
5. **库存查询**：查询指定药品的当前库存信息
6. **库存统计**：统计总药品数、低库存药品数、缺货药品数、库存总价值等

**模块特点**：
- 入库/出库操作自动更新药品库存，保证数据一致性
- 出库前自动检查库存充足性，防止负库存
- 使用事务管理确保入库/出库操作的原子性
- 支持多种出库原因记录，便于库存分析和追溯
- 提供丰富的查询和统计功能

### 4.3.2 模块性能要求

1. **响应时间要求**：
   - 入库操作响应时间 ≤ 500ms
   - 出库操作响应时间 ≤ 500ms（包含库存检查）
   - 库存记录查询响应时间 ≤ 300ms（1000条记录以内）
   - 库存统计响应时间 ≤ 1000ms

2. **并发性能要求**：
   - 支持至少50个并发入库/出库操作
   - 库存更新操作使用数据库锁机制保证数据一致性

3. **数据一致性要求**：
   - 入库/出库操作必须使用事务管理，保证原子性
   - 库存数量必须实时准确，不允许出现负库存

### 4.3.3 模块接口设计

#### 4.3.3.1 库存管理接口（StockController）

**（1）药品入库接口**
- **接口路径**：`POST /api/stock/in`
- **权限要求**：管理员或药师
- **请求参数**：
  ```json
  {
    "medicineId": "long",        // 药品ID，必填
    "batchNumber": "string",     // 批次号，可选
    "quantity": "integer",       // 入库数量，必填，>0
    "supplier": "string",        // 供应商，可选
    "inDate": "date"             // 入库日期，可选，默认当前日期
  }
  ```
- **响应数据**：入库记录对象（包含ID、操作员等信息）
- **业务逻辑**：
  - 验证药品是否存在
  - 保存入库记录
  - 自动更新药品库存（增加）
- **错误响应**：
  - 400：参数验证失败或药品不存在
  - 403：权限不足

**（2）药品出库接口**
- **接口路径**：`POST /api/stock/out`
- **权限要求**：管理员或药师
- **请求参数**：
  ```json
  {
    "medicineId": "long",        // 药品ID，必填
    "batchNumber": "string",     // 批次号，可选
    "quantity": "integer",       // 出库数量，必填，>0
    "reason": "string",          // 出库原因，可选，支持ENUM值或中文
    "outDate": "date"            // 出库日期，可选，默认当前日期
  }
  ```
- **响应数据**：出库记录对象（包含ID、操作员等信息）
- **业务逻辑**：
  - 验证药品是否存在
  - 检查库存是否充足（库存数量 ≥ 出库数量）
  - 保存出库记录
  - 自动更新药品库存（减少）
- **错误响应**：
  - 400：参数验证失败、药品不存在或库存不足
  - 403：权限不足

**（3）获取入库记录列表接口**
- **接口路径**：`GET /api/stock/in`
- **权限要求**：所有认证用户
- **请求参数**：
  - `medicineId`: 药品ID，可选（筛选指定药品的入库记录）
  - `startDate`: 开始日期，可选（格式：yyyy-MM-dd）
  - `endDate`: 结束日期，可选（格式：yyyy-MM-dd）
  - `page`: 页码，默认0
  - `size`: 每页大小，默认10
- **响应数据**：分页入库记录列表
- **说明**：支持按药品ID或日期范围筛选

**（4）获取出库记录列表接口**
- **接口路径**：`GET /api/stock/out`
- **权限要求**：所有认证用户
- **请求参数**：同入库记录列表接口
- **响应数据**：分页出库记录列表

**（5）查询药品库存接口**
- **接口路径**：`GET /api/stock/medicine/{medicineId}`
- **权限要求**：所有认证用户
- **路径参数**：`medicineId` - 药品ID
- **响应数据**：药品信息（包含库存数量）
- **错误响应**：
  - 404：药品不存在

**（6）库存统计接口**
- **接口路径**：`GET /api/stock/statistics`
- **权限要求**：所有认证用户
- **响应数据**：
  ```json
  {
    "totalMedicines": "integer",      // 总药品数
    "lowStockCount": "integer",       // 低库存药品数（库存 ≤ 安全库存）
    "outOfStockCount": "integer",     // 缺货药品数（库存 = 0）
    "totalStockValue": "integer"      // 库存总价值（元）
  }
  ```

#### 4.3.3.2 数据传递对象（DTO）

**实体类**（直接使用实体类作为DTO）：
- `StockIn`：入库记录实体
- `StockOut`：出库记录实体

**字段说明**：
- `StockOut.reason`：出库原因（ENUM类型：'prescription', 'loss', 'expired', 'other'）
  - JSON序列化时自动转换为中文（处方发药、盘亏、过期、其他）

#### 4.3.3.3 关联数据结构

**数据库表**：
- `stock_in`表：存储入库记录
  - 主键：`id`
  - 外键：`medicine_id` → `medicine.id`
  - 字段：medicine_id, batch_number, quantity, supplier, in_date, operator, create_time
  - 索引：medicine_id, in_date（用于查询优化）

- `stock_out`表：存储出库记录
  - 主键：`id`
  - 外键：`medicine_id` → `medicine.id`
  - 字段：medicine_id, batch_number, quantity, out_date, operator, reason, create_time
  - 索引：medicine_id, out_date（用于查询优化）

- `medicine`表：存储药品信息（包括库存数量）
  - 字段：stock_quantity（库存数量，通过入库/出库操作自动更新）

**相关模块调用**：
- 调用`StockInService`进行入库业务逻辑处理
- 调用`StockOutService`进行出库业务逻辑处理
- 调用`MedicineService.updateStock()`更新药品库存
- 被`PrescriptionService`调用，用于处方发药时的出库操作

### 4.3.4 用户界面设计

#### 4.3.4.1 库存管理界面（管理员/药师）

**界面布局**：
- 顶部：统计卡片（药品总数、库存预警、缺货数量、库存总值）
- 工具栏："药品入库"和"药品出库"按钮
- 中间：标签页（批次管理、入库记录、出库记录）
- 标签页内容：记录列表表格、搜索筛选条件、分页控件

**入库记录标签页**：
- 表格列：ID、药品ID、批次号、数量、供应商、操作人、入库日期、创建时间
- 操作按钮："药品入库"

**出库记录标签页**：
- 表格列：ID、药品ID、批次号、数量、出库原因、操作人、出库日期、创建时间
- 操作按钮："药品出库"

#### 4.3.4.2 入库操作界面

**操作步骤**：

1. 点击"药品入库"按钮，弹出表单对话框
2. 填写入库信息：
   - 药品ID（数字输入框，必填）
   - 批次号（文本输入框，可选）
   - 数量（数字输入框，必填，最小值1）
   - 供应商（文本输入框，可选）
   - 入库日期（日期选择器，默认当前日期）
3. 点击"确定"保存
4. 系统自动更新药品库存
5. 保存成功后关闭对话框并刷新列表

**注意事项**：
- 药品ID、数量为必填项
- 数量必须大于0
- 入库成功后自动增加药品库存数量
- 操作员自动设置为当前登录用户

#### 4.3.4.3 出库操作界面

**操作步骤**：

1. 点击"药品出库"按钮，弹出表单对话框
2. 填写出库信息：
   - 药品ID（数字输入框，必填）
   - 批次号（文本输入框，可选）
   - 数量（数字输入框，必填，最小值1，最大值不超过当前库存）
   - 出库原因（下拉选择框：处方发药、盘亏、过期、其他）
   - 出库日期（日期选择器，默认当前日期）
3. 点击"确定"保存
4. 系统检查库存是否充足
5. 如果库存充足，自动扣减药品库存
6. 保存成功后关闭对话框并刷新列表

**注意事项**：
- 药品ID、数量、出库原因为必填项
- 数量必须大于0，且不能超过当前库存
- 库存不足时显示错误提示："库存不足，当前库存：X，请求出库：Y"
- 出库成功后自动减少药品库存数量
- 操作员自动设置为当前登录用户

### 4.3.5 程序结构设计

#### 4.3.5.1 设计类图

**核心类**：

```
StockController
├── StockInService
├── StockOutService
├── MedicineService
└── SecurityContextHolder (权限验证)

StockInService
├── StockInRepository
├── MedicineService
└── BusinessException (异常处理)

StockOutService
├── StockOutRepository
├── MedicineService
├── StockInsufficientException (库存不足异常)
└── BusinessException (异常处理)

MedicineService
└── updateStock(Long medicineId, Integer quantity)
```

**类职责说明**：

- **StockController**：处理库存管理相关HTTP请求（入库、出库、记录查询、库存统计）
- **StockInService**：入库业务逻辑处理（创建入库记录、自动更新库存）
- **StockOutService**：出库业务逻辑处理（创建出库记录、检查库存充足性、自动更新库存）
- **StockInRepository / StockOutRepository**：库存记录数据访问层

#### 4.3.5.2 数据结构描述

**StockIn实体类**：
```java
public class StockIn {
    private Long id;                    // 入库记录ID（主键）
    private Long medicineId;            // 药品ID（外键）
    private String batchNumber;         // 批次号
    private Integer quantity;           // 入库数量（>0）
    private String supplier;            // 供应商
    private LocalDate inDate;           // 入库日期
    private String operator;            // 操作员
    private LocalDateTime createTime;   // 创建时间
}
```

**StockOut实体类**：
```java
public class StockOut {
    private Long id;                    // 出库记录ID（主键）
    private Long medicineId;            // 药品ID（外键）
    private String batchNumber;         // 批次号
    private Integer quantity;           // 出库数量（>0）
    private LocalDate outDate;          // 出库日期
    private String operator;            // 操作员
    private String reason;              // 出库原因（ENUM: prescription/loss/expired/other）
    private LocalDateTime createTime;   // 创建时间
}
```

### 4.3.6 算法及流程逻辑

#### 4.3.6.1 药品入库流程

**流程图描述**：

```
开始
  ↓
接收入库请求（medicineId, quantity等）
  ↓
验证请求参数（@Valid）
  ↓
检查权限（管理员或药师）
  ↓
权限是否满足？
  ├─ 否 → 返回403 Forbidden → 结束
  ↓ 是
根据medicineId查询药品
  ↓
药品是否存在？
  ├─ 否 → 抛出MedicineNotFoundException → 返回404 → 结束
  ↓ 是
设置入库日期（如果未设置，使用当前日期）
  ↓
设置操作员（当前登录用户）
  ↓
创建StockIn对象
  ↓
保存入库记录到数据库（@Transactional）
  ↓
调用MedicineService.updateStock(medicineId, quantity)更新库存
  ↓
返回入库记录（包含ID）
  ↓
结束
```

**关键算法**：

1. **库存更新算法**：
   ```
   新库存数量 = 原库存数量 + 入库数量
   使用MedicineService.updateStock(medicineId, quantity)
   其中quantity为正数，表示增加库存
   ```

#### 4.3.6.2 药品出库流程

**流程图描述**：

```
开始
  ↓
接收出库请求（medicineId, quantity, reason等）
  ↓
验证请求参数（@Valid）
  ↓
检查权限（管理员或药师）
  ↓
权限是否满足？
  ├─ 否 → 返回403 Forbidden → 结束
  ↓ 是
根据medicineId查询药品
  ↓
药品是否存在？
  ├─ 否 → 抛出MedicineNotFoundException → 返回404 → 结束
  ↓ 是
检查当前库存数量
  ↓
库存数量 ≥ 出库数量？
  ├─ 否 → 抛出StockInsufficientException → 返回400 → 结束
  ↓ 是
转换reason值（如果是中文，转换为ENUM值）
  ↓
设置出库日期（如果未设置，使用当前日期）
  ↓
设置操作员（当前登录用户）
  ↓
创建StockOut对象
  ↓
保存出库记录到数据库（@Transactional）
  ↓
调用MedicineService.updateStock(medicineId, -quantity)更新库存
  ↓
返回出库记录（包含ID）
  ↓
结束
```

**关键算法**：

1. **库存充足性检查算法**：
   ```
   当前库存数量 = Medicine.stockQuantity
   如果 当前库存数量 < 出库数量
       则 抛出StockInsufficientException
   ```

2. **库存扣减算法**：
   ```
   新库存数量 = 原库存数量 - 出库数量
   使用MedicineService.updateStock(medicineId, -quantity)
   其中quantity为正数，但传入负数（-quantity）表示减少库存
   ```

3. **出库原因转换算法**：
   ```
   如果reason包含"处方"或"发药" → "prescription"
   如果reason包含"盘亏"或"丢失" → "loss"
   如果reason包含"过期" → "expired"
   否则 → "other"
   ```

#### 4.3.6.3 库存统计流程

**流程图描述**：

```
开始
  ↓
调用MedicineService.findAll()获取所有药品
  ↓
初始化统计变量：
  totalMedicines = 0
  lowStockCount = 0
  outOfStockCount = 0
  totalStockValue = 0
  ↓
遍历所有药品：
  totalMedicines++
  ↓
  如果 stockQuantity <= 0
     outOfStockCount++
  ↓
  如果 stockQuantity <= minStock 且 stockQuantity > 0
     lowStockCount++
  ↓
  totalStockValue += price * stockQuantity
  ↓
构建统计结果Map
  ↓
返回统计结果
  ↓
结束
```

### 4.3.7 其它说明

1. **事务管理**：
   - 入库/出库操作使用`@Transactional`注解，保证操作的原子性
   - 如果库存更新失败，入库/出库记录不会保存
   - 如果出库记录保存失败，库存不会扣减

2. **库存一致性**：
   - 库存数量只能通过入库/出库操作更新，不允许直接修改
   - 出库前必须检查库存充足性，防止负库存
   - 使用数据库事务保证库存更新的原子性

3. **性能优化**：
   - 库存记录表建立索引（medicine_id, in_date, out_date），提升查询性能
   - 库存统计可以定期缓存，减少实时计算（可选的优化方案）

4. **业务规则**：
   - 入库数量必须大于0
   - 出库数量必须大于0，且不能超过当前库存
   - 出库原因必须记录，便于库存分析和追溯
   - 操作员自动记录，便于责任追溯

5. **错误处理**：
   - 使用`StockInsufficientException`处理库存不足的情况
   - 使用`MedicineNotFoundException`处理药品不存在的情况
   - 统一的错误响应格式（使用ApiResponse）

6. **扩展性考虑**：
   - 支持批量入库/出库（可选的扩展功能）
   - 支持库存盘点功能（可选的扩展功能）
   - 支持库存预警通知（可选的扩展功能）

---

## 4.4 处方管理模块设计说明

**模块标识符**：M-PRES

### 4.4.1 模块功能描述

处方管理模块是医院药房智能管理系统的核心业务模块，负责处方的录入、查询、审核、发药和取消等全生命周期管理。本模块与药品管理模块、库存管理模块、审核管理模块紧密集成，实现处方的完整业务流程。

**主要功能**：
1. **处方录入**：医生录入处方信息，包括患者信息、诊断信息、处方明细（药品、用量、频次等）
2. **处方查询**：支持按状态、患者姓名、日期等条件查询处方列表
3. **处方详情查看**：查看处方的完整信息，包括主表信息和明细列表
4. **处方提交审核**：将处方提交给Python审核服务进行自动审核
5. **处方人工审核**：药师对处方进行人工审核，给出审核结果和建议
6. **处方发药**：药师对已通过审核的处方进行发药操作，自动扣减库存
7. **处方取消**：取消未审核或审核中的处方

**模块特点**：
- 处方号自动生成，保证唯一性
- 支持多种处方状态流转（未审核→审核中→已通过/已拒绝→已发药/已取消）
- 与Python审核服务集成，实现自动审核
- 发药操作自动关联库存管理，扣减药品库存
- 处方明细支持多个药品，每个药品包含用法用量、频次、天数等详细信息

### 4.4.2 模块性能要求

1. **响应时间要求**：
   - 处方创建响应时间 ≤ 800ms
   - 处方查询响应时间 ≤ 500ms（1000条记录以内）
   - 处方详情查询响应时间 ≤ 300ms
   - 处方提交审核响应时间 ≤ 5000ms（包含Python服务调用）
   - 处方发药响应时间 ≤ 1000ms（包含库存检查）

2. **并发性能要求**：
   - 支持至少50个并发处方创建请求
   - 支持至少30个并发审核请求

3. **数据一致性要求**：
   - 处方创建和明细保存必须使用事务管理，保证原子性
   - 发药操作必须使用事务管理，保证库存扣减和状态更新的一致性

### 4.4.3 模块接口设计

#### 4.4.3.1 处方管理接口（PrescriptionController）

**（1）创建处方接口**
- **接口路径**：`POST /api/prescriptions`
- **权限要求**：医生或管理员
- **请求参数**：
  ```json
  {
    "patientName": "string",           // 患者姓名，必填
    "patientAge": "integer",           // 患者年龄，可选
    "patientGender": "string",         // 患者性别，可选
    "patientSymptoms": "string",       // 患者症状/病症，可选
    "diagnosis": "string",             // 诊断，可选
    "patientConditions": "string",     // 患者疾病状况，可选
    "allergies": "string",             // 过敏史，可选
    "department": "string",            // 科室，可选
    "createDate": "date",              // 处方日期，可选，默认当前日期
    "details": [                       // 处方明细列表，必填，至少1条
      {
        "medicineId": "long",          // 药品ID，必填
        "quantity": "integer",         // 数量，必填，>0
        "dosage": "string",            // 用法用量，可选
        "frequency": "string",         // 频次，可选
        "days": "integer"              // 用药天数，可选，默认1
      }
    ]
  }
  ```
- **响应数据**：创建的处方对象（包含自动生成的处方号）
- **业务逻辑**：
  - 自动生成处方号（格式：P + 日期 + 随机字符串）
  - 验证处方明细不为空
  - 保存处方主表和明细（事务管理）
  - 设置初始状态为"未审核"
- **错误响应**：
  - 400：参数验证失败或处方明细为空
  - 403：权限不足

**（2）获取处方列表接口**
- **接口路径**：`GET /api/prescriptions`
- **权限要求**：所有认证用户
- **请求参数**：
  - `status`: 处方状态，可选（未审核、审核中、已通过、已拒绝、已发药、已取消）
  - `startDate`: 开始日期，可选（格式：yyyy-MM-dd）
  - `endDate`: 结束日期，可选（格式：yyyy-MM-dd）
  - `page`: 页码，默认0
  - `size`: 每页大小，默认10
- **响应数据**：分页处方列表（按创建时间倒序）
- **说明**：支持按状态筛选，支持按日期范围筛选

**（3）获取处方详情接口**
- **接口路径**：`GET /api/prescriptions/{id}`
- **权限要求**：所有认证用户
- **路径参数**：`id` - 处方ID
- **响应数据**：
  ```json
  {
    "prescription": {处方主表信息},
    "details": [{处方明细列表}]
  }
  ```
- **错误响应**：
  - 404：处方不存在

**（4）提交审核接口**
- **接口路径**：`POST /api/prescriptions/{id}/submit-audit`
- **权限要求**：医生或管理员
- **路径参数**：`id` - 处方ID
- **响应数据**：成功消息
- **业务逻辑**：
  - 检查处方状态是否为"未审核"
  - 调用Python审核服务进行自动审核
  - 保存审核记录
  - 更新处方状态和审核结果
- **错误响应**：
  - 400：处方状态不正确或Python服务不可用
  - 403：权限不足
  - 404：处方不存在

**（5）人工审核接口**
- **接口路径**：`POST /api/prescriptions/{id}/audit`
- **权限要求**：药师或管理员
- **路径参数**：`id` - 处方ID
- **请求参数**：
  ```json
  {
    "auditResult": "string",    // 审核结果：通过/拒绝/警告
    "suggestions": "string"     // 审核建议，可选
  }
  ```
- **响应数据**：成功消息
- **业务逻辑**：
  - 将中文审核结果转换为ENUM值保存到数据库
  - 保存审核记录（类型：人工审核）
  - 更新处方状态和审核结果
- **错误响应**：
  - 400：参数验证失败
  - 403：权限不足
  - 404：处方不存在

**（6）发药接口**
- **接口路径**：`POST /api/prescriptions/{id}/dispense`
- **权限要求**：药师或管理员
- **路径参数**：`id` - 处方ID
- **响应数据**：成功消息
- **业务逻辑**：
  - 检查处方状态是否为"已通过"
  - 获取处方明细
  - 为每个明细创建出库记录并扣减库存
  - 更新处方状态为"已发药"
- **错误响应**：
  - 400：处方状态不正确或库存不足
  - 403：权限不足
  - 404：处方不存在

**（7）取消处方接口**
- **接口路径**：`POST /api/prescriptions/{id}/cancel`
- **权限要求**：医生或管理员
- **路径参数**：`id` - 处方ID
- **响应数据**：成功消息
- **业务逻辑**：
  - 检查处方状态（只能取消"未审核"或"审核中"状态的处方）
  - 更新处方状态为"已取消"
- **错误响应**：
  - 400：处方状态不允许取消
  - 403：权限不足
  - 404：处方不存在

#### 4.4.3.2 数据传递对象（DTO）

**请求DTO**：
- `PrescriptionCreateRequest`：创建处方请求（已废弃，改为使用Map手动解析）
- `PrescriptionAuditRequest`：审核请求，包含auditResult和suggestions

**响应DTO**：
- `PrescriptionResponse`：处方信息响应（使用Prescription实体类）

#### 4.4.3.3 关联数据结构

**数据库表**：
- `prescription`表：存储处方主表信息
  - 主键：`id`
  - 唯一索引：`prescription_number`（处方号）
  - 字段：prescription_number, patient_name, patient_age, patient_gender, patient_symptoms, diagnosis, patient_conditions, allergies, doctor_name, department, create_date, status, audit_result, audit_time, create_time, update_time
  - 索引：status, create_date（用于查询优化）

- `prescription_detail`表：存储处方明细
  - 主键：`id`
  - 外键：`prescription_id` → `prescription.id`, `medicine_id` → `medicine.id`
  - 字段：prescription_id, medicine_id, quantity, dosage, frequency, days, create_time
  - 索引：prescription_id（用于查询优化）

**相关模块调用**：
- 调用`PrescriptionService`进行处方业务逻辑处理
- 调用`PrescriptionDetailService`进行处方明细业务逻辑处理
- 调用`MedicineService`查询药品信息
- 调用`StockOutService`进行发药时的出库操作
- 调用`AuditRecordService`保存审核记录
- 调用`AuditServiceClient`调用Python审核服务

### 4.4.4 用户界面设计

#### 4.4.4.1 处方管理界面（医生）

**界面布局**：
- 顶部：标题"我的处方"、"新建处方"按钮
- 搜索栏：状态筛选、患者姓名搜索
- 中间：处方列表表格
- 表格列：ID、处方号、患者姓名、年龄、性别、科室、状态、处方日期、操作（查看/取消）
- 底部：分页控件

**操作步骤**：

**新建处方**：
1. 点击"新建处方"按钮，弹出表单对话框
2. 填写患者信息：
   - 患者姓名（必填）
   - 患者年龄（可选）
   - 患者性别（可选，下拉选择：男/女）
   - 科室（可选）
   - 患者症状/病症（可选，文本域）
   - 诊断（可选）
   - 过敏史（可选，文本域）
3. 添加处方明细：
   - 点击"添加药品"按钮，添加一行明细
   - 选择药品（下拉选择框，支持搜索，显示药品名称和规格）
   - 填写数量（数字输入框，最小值1）
   - 填写用法用量（可选，如：每次2粒）
   - 填写频次（可选，如：每日3次）
   - 填写用药天数（可选，默认1）
   - 可以添加多行明细
   - 可以删除明细行
4. 点击"确定"保存处方
5. 保存成功后关闭对话框并刷新列表

**查看处方**：
1. 点击处方行的"查看"按钮，弹出详情对话框
2. 显示处方完整信息：
   - 患者信息（姓名、年龄、性别、科室等）
   - 诊断信息（症状、诊断、过敏史）
   - 处方明细列表（药品名称、数量、用法用量、频次、天数）
   - 处方状态和审核结果（如有）
3. 点击"关闭"关闭对话框

**取消处方**：
1. 点击处方行的"取消"按钮（仅"未审核"状态显示）
2. 弹出确认对话框："确定要取消该处方吗？"
3. 点击"确定"执行取消，点击"取消"取消操作
4. 取消成功后刷新列表

#### 4.4.4.2 处方管理界面（药师）

**界面布局**：
- 顶部：标题"处方管理"、状态筛选标签（全部、待审核、已通过、已拒绝）
- 中间：处方列表表格
- 表格列：ID、处方号、患者姓名、科室、状态、处方日期、操作（查看/审核/发药）
- 底部：分页控件

**操作步骤**：

**审核处方**：
1. 在"待审核"标签页查看待审核处方列表
2. 点击处方行的"审核"按钮，弹出审核对话框
3. 查看处方详情（患者信息、诊断、处方明细）
4. 填写审核结果：
   - 审核结果（必填，下拉选择：通过/拒绝/警告）
   - 审核建议（可选，文本域）
5. 点击"确定"提交审核
6. 审核成功后关闭对话框并刷新列表

**发药操作**：
1. 在"已通过"标签页查看已通过审核的处方列表
2. 点击处方行的"发药"按钮
3. 弹出确认对话框："确定要为该处方发药吗？"
4. 点击"确定"执行发药
5. 系统自动扣减库存，更新处方状态为"已发药"
6. 发药成功后显示成功提示，刷新列表

**查看处方详情**：
1. 点击处方行的"查看"按钮，弹出详情对话框
2. 显示处方完整信息（包括审核记录）
3. 点击"关闭"关闭对话框

### 4.4.5 程序结构设计

#### 4.4.5.1 设计类图

**核心类**：

```
PrescriptionController
├── PrescriptionService
├── PrescriptionDetailService
├── AuditRecordService
└── SecurityContextHolder (权限验证)

PrescriptionService
├── PrescriptionRepository
├── PrescriptionDetailService
├── AuditRecordService
├── StockOutService
├── AuditServiceClient (调用Python审核服务)
├── MedicineService
└── BusinessException / PrescriptionNotFoundException

PrescriptionDetailService
├── PrescriptionDetailRepository
└── JPA Repository
```

**类职责说明**：

- **PrescriptionController**：处理处方管理相关HTTP请求（创建、查询、审核、发药、取消）
- **PrescriptionService**：处方业务逻辑处理（创建处方、提交审核、发药、状态管理）
- **PrescriptionDetailService**：处方明细业务逻辑处理（保存、查询明细）
- **PrescriptionRepository / PrescriptionDetailRepository**：处方数据访问层

#### 4.4.5.2 数据结构描述

**Prescription实体类**：
```java
public class Prescription {
    private Long id;                    // 处方ID（主键）
    private String prescriptionNumber;  // 处方号（唯一）
    private String patientName;         // 患者姓名
    private Integer patientAge;         // 患者年龄
    private String patientGender;       // 患者性别
    private String patientSymptoms;     // 患者症状/病症
    private String diagnosis;           // 诊断
    private String patientConditions;   // 患者疾病状况
    private String allergies;           // 过敏史
    private String doctorName;          // 医生姓名
    private String department;          // 科室
    private LocalDate createDate;       // 处方日期
    private String status;              // 状态（未审核、审核中、已通过、已拒绝、已发药、已取消）
    private String auditResult;         // 审核结果
    private LocalDateTime auditTime;    // 审核时间
    private LocalDateTime createTime;   // 创建时间
    private LocalDateTime updateTime;   // 更新时间
    private List<PrescriptionDetail> details; // 处方明细列表
}
```

**PrescriptionDetail实体类**：
```java
public class PrescriptionDetail {
    private Long id;                    // 明细ID（主键）
    private Long prescriptionId;        // 处方ID（外键）
    private Long medicineId;            // 药品ID（外键）
    private Integer quantity;           // 数量（>0）
    private String dosage;              // 用法用量
    private String frequency;           // 频次
    private Integer days;               // 用药天数
    private LocalDateTime createTime;   // 创建时间
}
```

### 4.4.6 算法及流程逻辑

#### 4.4.6.1 处方创建流程

**流程图描述**：

```
开始
  ↓
接收创建处方请求（包含患者信息和明细列表）
  ↓
验证请求参数
  ↓
检查权限（医生或管理员）
  ↓
权限是否满足？
  ├─ 否 → 返回403 Forbidden → 结束
  ↓ 是
检查处方明细是否为空
  ↓
明细是否为空？
  ├─ 是 → 返回"处方明细不能为空" → 结束
  ↓ 否
生成处方号（如果未提供）
  格式：P + yyyyMMdd + 8位随机字符串
  ↓
创建Prescription对象
  ↓
设置初始状态为"未审核"
设置创建日期（如果未设置，使用当前日期）
设置医生姓名（当前登录用户）
  ↓
保存处方主表到数据库（@Transactional）
  ↓
遍历明细列表：
  设置prescriptionId = 保存后的处方ID
  ↓
保存处方明细列表到数据库
  ↓
返回创建的处方对象
  ↓
结束
```

**关键算法**：

1. **处方号生成算法**：
   ```
   日期部分 = 当前日期格式化（yyyyMMdd）
   随机部分 = UUID的前8位转大写
   处方号 = "P" + 日期部分 + 随机部分
   例如：P20241212A1B2C3D4
   ```

#### 4.4.6.2 处方提交审核流程

**流程图描述**：

```
开始
  ↓
接收提交审核请求（处方ID）
  ↓
检查权限（医生或管理员）
  ↓
根据ID查询处方
  ↓
处方是否存在？
  ├─ 否 → 抛出PrescriptionNotFoundException → 返回404 → 结束
  ↓ 是
检查处方状态是否为"未审核"
  ↓
状态是否正确？
  ├─ 否 → 返回"只能提交未审核状态的处方" → 结束
  ↓ 是
检查Python审核服务是否可用
  ↓
服务是否可用？
  ├─ 否 → 返回"审核服务不可用" → 结束
  ↓ 是
更新处方状态为"审核中"
  ↓
获取处方明细列表
  ↓
获取每个明细对应的药品信息
  ↓
构建审核请求payload（包含处方信息和药品信息）
  ↓
调用Python审核服务（AuditServiceClient.auditPrescription）
  ↓
处理审核结果：
  转换审核结果为中文状态
  转换审核结果为处方状态
  构建问题描述字符串
  ↓
保存审核记录（AuditRecord）
  类型：自动审核
  审核员：Python审核服务
  ↓
更新处方状态和审核结果
  ↓
返回成功响应
  ↓
结束

（如果出现异常，回退处方状态为"未审核"）
```

#### 4.4.6.3 处方发药流程

**流程图描述**：

```
开始
  ↓
接收发药请求（处方ID）
  ↓
检查权限（药师或管理员）
  ↓
根据ID查询处方
  ↓
处方是否存在？
  ├─ 否 → 抛出PrescriptionNotFoundException → 返回404 → 结束
  ↓ 是
检查处方状态是否为"已通过"
  ↓
状态是否正确？
  ├─ 否 → 返回"只能对已通过审核的处方进行发药" → 结束
  ↓ 是
获取处方明细列表
  ↓
明细是否为空？
  ├─ 是 → 返回"处方明细为空" → 结束
  ↓ 否
遍历处方明细（@Transactional）：
  ↓
  创建StockOut对象
    设置medicineId、quantity
    设置outDate为当前日期
    设置reason为"prescription"（ENUM值）
    设置operator为当前登录用户
  ↓
  调用StockOutService.processStockOut(stockOut)
    检查库存是否充足
    如果库存不足，抛出StockInsufficientException
    如果库存充足，扣减库存
  ↓
  保存出库记录
  ↓
更新处方状态为"已发药"
  ↓
返回成功响应
  ↓
结束

（如果库存不足或出现异常，事务回滚，不扣减库存，不更新状态）
```

### 4.4.7 其它说明

1. **事务管理**：
   - 处方创建使用`@Transactional`，保证主表和明细的保存原子性
   - 发药操作使用`@Transactional`，保证出库记录保存和库存扣减的原子性
   - 如果操作失败，事务回滚，保证数据一致性

2. **状态流转规则**：
   - 未审核 → 审核中（提交审核）
   - 审核中 → 已通过/已拒绝（审核完成）
   - 已通过 → 已发药（发药）
   - 未审核/审核中 → 已取消（取消）

3. **处方号唯一性**：
   - 处方号自动生成，使用日期+随机字符串保证唯一性
   - 数据库层面设置唯一索引，防止重复

4. **Python服务集成**：
   - 调用前检查服务可用性
   - 审核失败时回退处方状态
   - 审核结果转换为中文状态显示

5. **库存管理集成**：
   - 发药时自动创建出库记录
   - 自动检查库存充足性
   - 库存不足时阻止发药

6. **错误处理**：
   - 使用`PrescriptionNotFoundException`处理处方不存在
   - 使用`StockInsufficientException`处理库存不足
   - 使用`BusinessException`处理业务逻辑错误
   - 统一的错误响应格式（使用ApiResponse）

7. **扩展性考虑**：
   - 支持处方打印（可选的扩展功能）
   - 支持处方模板保存（可选的扩展功能）
   - 支持处方统计分析（可选的扩展功能）

---

## 4.5 审核管理模块设计说明

**模块标识符**：M-AUDIT

### 4.5.1 模块功能描述

审核管理模块负责处方审核记录的查询、统计和Python审核服务的集成调用。本模块支持自动审核（调用Python服务）和人工审核两种审核方式，记录每次审核的详细信息，为处方审核提供完整的追溯能力。

**主要功能**：
1. **审核记录查询**：支持按处方ID、审核结果等条件查询审核记录
2. **审核记录详情查看**：查看审核记录的完整信息（审核类型、结果、得分、问题、建议等）
3. **处方审核历史查询**：查询指定处方的所有审核记录（支持多次审核）
4. **审核统计**：统计总审核记录数、通过/拒绝数量、通过率、自动/人工审核数量等
5. **Python审核服务集成**：调用外部Python审核服务进行自动审核，处理审核结果
6. **审核服务健康检查**：检查Python审核服务的可用性

**模块特点**：
- 支持自动审核和人工审核两种审核方式
- 审核结果使用ENUM类型存储（pass/warning/reject），JSON序列化时转换为中文
- 审核记录与处方关联，支持多次审核历史追踪
- 集成Python审核服务，实现智能审核
- 提供丰富的审核统计数据，便于分析审核质量

### 4.5.2 模块性能要求

1. **响应时间要求**：
   - 审核记录查询响应时间 ≤ 300ms（1000条记录以内）
   - 审核记录详情查询响应时间 ≤ 200ms
   - 审核统计响应时间 ≤ 500ms
   - Python审核服务调用响应时间 ≤ 5000ms（包含网络传输和算法处理）

2. **并发性能要求**：
   - 支持至少30个并发审核记录查询请求
   - 支持至少10个并发Python审核服务调用

3. **外部服务要求**：
   - Python审核服务应具备高可用性（可用率≥95%）
   - Python审核服务超时时间设置为10秒
   - 服务不可用时应有友好的错误提示和降级处理

### 4.5.3 模块接口设计

#### 4.5.3.1 审核记录管理接口（AuditController）

**（1）获取审核记录列表接口**
- **接口路径**：`GET /api/audit/records`
- **权限要求**：管理员或药师
- **请求参数**：
  - `auditResult`: 审核结果，可选（通过/拒绝/警告）
  - `prescriptionId`: 处方ID，可选（筛选指定处方的审核记录）
  - `page`: 页码，默认0
  - `size`: 每页大小，默认10
- **响应数据**：分页审核记录列表
- **说明**：支持按审核结果或处方ID筛选

**（2）获取审核记录详情接口**
- **接口路径**：`GET /api/audit/records/{id}`
- **权限要求**：管理员或药师
- **路径参数**：`id` - 审核记录ID
- **响应数据**：审核记录详细信息
- **错误响应**：
  - 404：审核记录不存在

**（3）获取处方审核历史接口**
- **接口路径**：`GET /api/audit/records/prescription/{prescriptionId}`
- **权限要求**：管理员或药师
- **路径参数**：`prescriptionId` - 处方ID
- **响应数据**：审核记录列表（按审核时间倒序）
- **说明**：返回指定处方的所有审核记录，用于查看审核历史

**（4）审核统计接口**
- **接口路径**：`GET /api/audit/statistics`
- **权限要求**：管理员或药师
- **响应数据**：
  ```json
  {
    "totalRecords": "integer",        // 总审核记录数
    "passedCount": "integer",         // 通过数量
    "rejectedCount": "integer",       // 拒绝数量
    "pendingCount": "integer",        // 待审核数量
    "autoAuditCount": "integer",      // 自动审核数量
    "manualAuditCount": "integer",    // 人工审核数量
    "passRate": "double"              // 通过率（百分比）
  }
  ```

#### 4.5.3.2 Python审核服务接口（AuditServiceClient）

**（1）审核处方接口（内部调用）**
- **方法**：`auditPrescription(Prescription, List<PrescriptionDetail>, List<Medicine>)`
- **服务URL**：`{auditServiceUrl}/api/prescription/audit`
- **请求方法**：POST
- **请求头**：`Content-Type: application/json`
- **请求体**：
  ```json
  {
    "prescription": {
      "prescription_number": "string",
      "doctor_name": "string",
      "department": "string",
      "create_date": "string",
      "patient": {
        "name": "string",
        "age": "integer",
        "gender": "string",
        "symptoms": "string",
        "conditions": "string",
        "allergies": "string",
        "diagnosis": "string"
      },
      "medicines": [
        {
          "name": "string",
          "generic_name": "string",
          "quantity": "integer",
          "dosage": "string",
          "frequency": "string",
          "days": "integer",
          "specification": "string",
          "category": "string",
          "indication": "string",
          "contraindication": "string",
          "interactions": "string"
        }
      ]
    }
  }
  ```
- **响应数据**：
  ```json
  {
    "success": true,
    "message": "审核成功",
    "data": {
      "result": "PASS|WARNING|REJECT",
      "score": "double (0-100)",
      "issues": [
        {
          "severity": "ERROR|WARNING|INFO",
          "issue_type": "string",
          "description": "string"
        }
      ],
      "suggestions": "string"
    }
  }
  ```

**（2）健康检查接口**
- **方法**：`isServiceAvailable()`
- **服务URL**：`{auditServiceUrl}/health`
- **请求方法**：GET
- **响应**：200状态码表示服务可用

#### 4.5.3.3 数据传递对象（DTO）

**响应DTO**：
- `AuditResultDto`：Python审核服务返回的审核结果
  - `result`: 审核结果（PASS/WARNING/REJECT）
  - `score`: 审核得分（0-100）
  - `issues`: 问题列表
  - `suggestions`: 建议

#### 4.5.3.4 关联数据结构

**数据库表**：
- `audit_record`表：存储审核记录
  - 主键：`id`
  - 外键：`prescription_id` → `prescription.id`
  - 字段：prescription_id, audit_type (ENUM: auto/manual), audit_result (ENUM: pass/warning/reject), audit_score, issues_found, suggestions, auditor, audit_time, create_time
  - 索引：prescription_id, audit_result（用于查询优化）

**相关模块调用**：
- 调用`AuditRecordService`进行审核记录业务逻辑处理
- 调用`PrescriptionService`获取处方信息（用于Python服务调用）
- 调用`MedicineService`获取药品信息（用于Python服务调用）
- 调用`PrescriptionDetailService`获取处方明细（用于Python服务调用）
- 被`PrescriptionService`调用，用于保存审核记录

### 4.5.4 用户界面设计

#### 4.5.4.1 审核记录管理界面（管理员/药师）

**界面布局**：
- 顶部：标题"审核记录"
- 搜索栏：审核类型筛选、审核结果筛选
- 中间：审核记录列表表格
- 表格列：ID、处方ID、审核类型、审核结果、得分、审核人、审核时间、发现问题、操作（查看）
- 底部：分页控件

**操作步骤**：

**查看审核记录列表**：
1. 进入审核记录管理页面
2. 默认显示所有审核记录列表（按审核时间倒序）
3. 可以按审核类型筛选（自动审核/人工审核）
4. 可以按审核结果筛选（通过/拒绝/警告）
5. 可以按处方ID搜索特定处方的审核记录

**查看审核记录详情**：
1. 点击审核记录行的"查看"按钮，弹出详情对话框
2. 显示审核记录完整信息：
   - 基本信息（记录ID、处方ID、审核类型、审核结果、审核得分）
   - 审核人信息（审核人、审核时间、创建时间）
   - 详细内容（发现问题、建议）
3. 点击"关闭"关闭对话框

**查看审核统计**：
1. 在审核记录管理页面顶部显示统计卡片（可选）
2. 统计信息包括：
   - 总审核记录数
   - 通过数量、拒绝数量、待审核数量
   - 自动审核数量、人工审核数量
   - 通过率（百分比）
3. 可以点击"查看详细统计"进入统计详情页面（可选功能）

#### 4.5.4.2 处方审核历史查看界面

**操作步骤**：

1. 在处方详情页面，点击"审核历史"标签页
2. 显示该处方的所有审核记录（按时间倒序）
3. 每条审核记录显示：
   - 审核类型（自动审核/人工审核）
   - 审核结果（通过/拒绝/警告）
   - 审核得分（如有）
   - 审核人
   - 审核时间
   - 发现问题（如有）
   - 建议（如有）
4. 可以点击"查看详情"查看完整审核记录

### 4.5.5 程序结构设计

#### 4.5.5.1 设计类图

**核心类**：

```
AuditController
├── AuditRecordService
└── SecurityContextHolder (权限验证)

AuditRecordService
├── AuditRecordRepository
└── BusinessException (异常处理)

AuditServiceClient
├── RestTemplate (HTTP客户端)
├── auditServiceUrl (配置)
└── BusinessException (异常处理)

AuditRecord (实体类)
├── @JsonGetter (JSON序列化转换)
└── JPA Entity
```

**类职责说明**：

- **AuditController**：处理审核记录相关HTTP请求（查询、详情、统计）
- **AuditRecordService**：审核记录业务逻辑处理（保存、查询、统计）
- **AuditServiceClient**：Python审核服务HTTP客户端（调用审核服务、健康检查）
- **AuditRecord**：审核记录实体类（包含ENUM到中文的JSON序列化转换）

#### 4.5.5.2 数据结构描述

**AuditRecord实体类**：
```java
public class AuditRecord {
    private Long id;                    // 审核记录ID（主键）
    private Long prescriptionId;        // 处方ID（外键）
    private String auditType;           // 审核类型（ENUM: auto/manual，JSON: 自动审核/人工审核）
    private String auditResult;         // 审核结果（ENUM: pass/warning/reject，JSON: 通过/警告/拒绝）
    private BigDecimal auditScore;      // 审核得分（0-100）
    private String issuesFound;         // 发现的问题
    private String suggestions;         // 建议
    private String auditor;             // 审核人
    private LocalDateTime auditTime;    // 审核时间
    private LocalDateTime createTime;   // 创建时间
}
```

**AuditResultDto类**：
```java
public class AuditResultDto {
    private String result;              // 审核结果（PASS/WARNING/REJECT）
    private Double score;               // 审核得分（0-100）
    private List<AuditIssueDto> issues; // 问题列表
    private String suggestions;         // 建议
}
```

### 4.5.6 算法及流程逻辑

#### 4.5.6.1 Python审核服务调用流程

**流程图描述**：

```
开始
  ↓
检查Python审核服务是否可用（isServiceAvailable）
  ↓
服务是否可用？
  ├─ 否 → 抛出BusinessException("审核服务不可用") → 结束
  ↓ 是
构建请求payload：
  构建患者信息（姓名、年龄、性别、症状、诊断、过敏史）
  构建药品列表（名称、规格、数量、用法、适应症、禁忌症等）
  构建处方信息（处方号、医生、科室、日期）
  ↓
设置HTTP请求头（Content-Type: application/json）
  ↓
发送POST请求到Python服务（/api/prescription/audit）
  ↓
接收响应
  ↓
检查响应状态码
  ↓
状态码是否为2xx？
  ├─ 否 → 抛出RestClientException → 结束
  ↓ 是
解析响应体（AuditServiceResponse）
  ↓
检查success字段
  ↓
success是否为true？
  ├─ 否 → 抛出BusinessException(响应中的message) → 结束
  ↓ 是
提取data字段（AuditResultDto）
  ↓
返回AuditResultDto
  ↓
结束
```

**关键算法**：

1. **请求payload构建算法**：
   ```
   遍历处方明细列表：
     对于每个明细：
       获取对应的药品信息（通过medicineId）
       构建药品数据Map：
         - 基本信息：name, generic_name, specification, category
         - 处方信息：quantity, dosage, frequency, days
         - 临床信息：indication, contraindication, interactions
     添加到medicines列表
   
   构建患者数据Map：
     - 基本信息：name, age, gender
     - 临床信息：symptoms, conditions, allergies, diagnosis
   
   构建处方数据Map：
     - prescription_number, doctor_name, department, create_date
     - patient（患者数据）
     - medicines（药品列表）
   
   构建最终payload：
     prescription: 处方数据
   ```

2. **审核结果转换算法**：
   ```
   Python返回：PASS → 中文：通过，ENUM：pass
   Python返回：WARNING → 中文：警告，ENUM：warning
   Python返回：REJECT → 中文：拒绝，ENUM：reject
   ```

#### 4.5.6.2 审核记录查询流程

**流程图描述**：

```
开始
  ↓
接收查询请求（包含筛选条件）
  ↓
检查权限（管理员或药师）
  ↓
权限是否满足？
  ├─ 否 → 返回403 Forbidden → 结束
  ↓ 是
判断筛选条件：
  ↓
  如果prescriptionId不为空：
    调用AuditRecordService.findByPrescriptionId(prescriptionId)
  ↓
  否则如果auditResult不为空：
    调用AuditRecordService.findByAuditResult(auditResult)
  ↓
  否则：
    调用AuditRecordService.findAll()
  ↓
对结果进行排序（按审核时间倒序）
  ↓
执行分页处理
  ↓
构建分页响应
  ↓
返回审核记录列表
  ↓
结束
```

#### 4.5.6.3 审核统计流程

**流程图描述**：

```
开始
  ↓
调用AuditRecordService.findAll()获取所有审核记录
  ↓
初始化统计变量：
  totalRecords = 0
  passedCount = 0
  rejectedCount = 0
  pendingCount = 0
  autoAuditCount = 0
  manualAuditCount = 0
  ↓
遍历所有审核记录：
  totalRecords++
  ↓
  检查auditResult字段：
    如果是"通过"或"已通过" → passedCount++
    如果是"拒绝"或"已拒绝" → rejectedCount++
    如果是"待审核" → pendingCount++
  ↓
  检查auditType字段：
    如果是"自动审核" → autoAuditCount++
    如果是"人工审核" → manualAuditCount++
  ↓
计算通过率：
  passRate = (passedCount / totalRecords) * 100
  ↓
构建统计结果Map：
  {
    totalRecords,
    passedCount,
    rejectedCount,
    pendingCount,
    autoAuditCount,
    manualAuditCount,
    passRate
  }
  ↓
返回统计结果
  ↓
结束
```

### 4.5.7 其它说明

1. **ENUM值处理**：
   - 数据库使用ENUM类型存储（audit_type: auto/manual，audit_result: pass/warning/reject）
   - Java实体类使用String类型，存储ENUM值
   - JSON序列化时使用`@JsonGetter`方法转换为中文显示
   - 前端传入中文值，后端转换为ENUM值保存

2. **Python服务集成**：
   - 使用RestTemplate进行HTTP调用
   - 服务URL通过配置文件配置（audit.service.url）
   - 调用前检查服务可用性（健康检查）
   - 服务不可用时提供友好的错误提示
   - 支持超时设置（可配置）

3. **审核记录关联**：
   - 一个处方可以有多个审核记录（支持多次审核）
   - 查询处方审核历史时，按审核时间倒序排列
   - 最新审核记录作为当前审核结果

4. **审核统计**：
   - 统计基于所有审核记录计算
   - 通过率 = 通过数量 / 总记录数 * 100
   - 统计结果实时计算（未使用缓存）

5. **错误处理**：
   - 使用`BusinessException`处理业务逻辑错误
   - Python服务调用失败时记录详细日志
   - 服务不可用时有降级处理（不阻塞处方流程）

6. **性能优化**：
   - 审核记录表建立索引（prescription_id, audit_result）
   - 大量数据时可以添加统计缓存（可选的优化方案）
   - Python服务调用支持异步处理（可选的优化方案）

7. **扩展性考虑**：
   - 支持多种审核服务集成（可选的扩展功能）
   - 支持审核规则配置（可选的扩展功能）
   - 支持审核质量分析报表（可选的扩展功能）

---

# 5 总结体会

通过本次软件设计实践课程，我们小组完成了"医院药房智能管理系统"的设计与开发工作。在整个项目过程中，我们从需求分析、系统设计、编码实现到测试部署，经历了完整的软件开发生命周期，收获颇丰。本章节将对项目完成情况进行总结，并分享我们在开发过程中的技术收获、遇到的困难和心得体会。

## 5.1 项目完成情况总结

### 5.1.1 功能完成情况

本项目按照课程设计要求，完成了以下核心功能模块：

**（1）用户认证与权限管理模块**
- ✅ 实现了基于JWT的用户登录认证机制
- ✅ 完成了基于角色的访问控制（RBAC），支持管理员、医生、护士、药师四种角色
- ✅ 实现了用户信息的增删改查功能
- ✅ 支持密码加密存储（BCrypt）和令牌刷新机制

**（2）药品管理模块**
- ✅ 完成了药品信息的完整CRUD操作
- ✅ 实现了药品搜索和分类查询功能
- ✅ 支持低库存预警功能
- ✅ 完成了药品详细信息管理（包括适应症、禁忌症、药物相互作用等）

**（3）库存管理模块**
- ✅ 实现了药品入库管理功能，自动更新库存数量
- ✅ 实现了药品出库管理功能，支持多种出库原因记录
- ✅ 完成了库存充足性检查，防止负库存
- ✅ 实现了库存统计功能（总药品数、低库存数、缺货数、库存总价值）

**（4）处方管理模块**
- ✅ 完成了处方录入功能，支持患者信息、诊断信息和处方明细管理
- ✅ 实现了处方查询和详情查看功能
- ✅ 完成了处方状态流转管理（未审核→审核中→已通过/已拒绝→已发药）
- ✅ 实现了处方发药功能，自动关联库存管理并扣减库存
- ✅ 支持处方取消功能

**（5）审核管理模块**
- ✅ 实现了Python审核服务集成，支持自动审核
- ✅ 完成了人工审核功能
- ✅ 实现了审核记录查询和统计功能
- ✅ 支持处方审核历史追踪

### 5.1.2 技术实现情况

**后端实现**：
- ✅ 采用Spring Boot框架构建RESTful API服务
- ✅ 使用Spring Data JPA进行数据持久化，简化数据库操作
- ✅ 集成Spring Security实现安全认证和授权
- ✅ 使用JWT无状态认证，支持分布式部署
- ✅ 实现了全局异常处理和统一响应格式
- ✅ 完成了37个RESTful API接口（超出计划要求15-20个接口）

**前端实现**：
- ✅ 采用Vue3框架构建单页应用
- ✅ 使用Element Plus组件库提供丰富的UI组件
- ✅ 使用Pinia进行状态管理
- ✅ 实现了响应式布局，支持多角色用户界面
- ✅ 完成了前后端API集成和错误处理

**数据库设计**：
- ✅ 设计了7张核心数据表（users, medicine, stock_in, stock_out, prescription, prescription_detail, audit_record）
- ✅ 建立了完整的外键约束和索引优化
- ✅ 使用ENUM类型存储状态和类型字段
- ✅ 实现了数据完整性约束和验证

**外部服务集成**：
- ✅ 完成了Python审核服务的HTTP客户端封装
- ✅ 实现了服务健康检查和错误处理机制
- ✅ 完成了审核数据的格式转换和结果处理

### 5.1.3 项目成果

本项目最终交付的成果包括：
- 完整的源代码（后端Java代码、前端Vue3代码、Python审核服务代码）
- 数据库初始化脚本和更新脚本
- 完整的课程设计报告（包括概述、系统分析、总体设计、详细设计）
- API接口文档
- 项目部署和运行文档

## 5.2 技术收获

### 5.2.1 后端开发技术

通过本次项目实践，我们在后端开发方面获得了以下技术提升：

**（1）Spring Boot框架**
- 深入理解了Spring Boot的自动配置机制和起步依赖
- 掌握了Spring Boot的配置文件管理（application.yml）
- 学会了使用Spring Boot的内嵌服务器和打包部署

**（2）Spring Data JPA**
- 理解了JPA的ORM映射原理和实体关系设计
- 掌握了JPA的Repository接口定义和查询方法命名规范
- 学会了使用`@Query`注解编写自定义查询
- 理解了JPA的懒加载（Lazy Loading）和急加载（Eager Loading）

**（3）Spring Security**
- 理解了认证和授权的区别
- 掌握了JWT的工作原理和实现方式
- 学会了配置Spring Security的过滤器链和权限控制
- 理解了基于角色的访问控制（RBAC）的实现

**（4）事务管理**
- 深入理解了数据库事务的ACID特性
- 掌握了`@Transactional`注解的使用场景和注意事项
- 学会了处理事务回滚和异常传播

### 5.2.2 前端开发技术

**（1）Vue3框架**
- 理解了Vue3的Composition API和响应式系统
- 掌握了组件的生命周期和状态管理
- 学会了使用`ref`、`reactive`、`computed`等API

**（2）Element Plus组件库**
- 学会了使用Element Plus的表格、表单、对话框等组件
- 掌握了表单验证和错误处理
- 理解了组件的事件绑定和数据双向绑定

**（3）状态管理**
- 理解了Pinia的状态管理原理
- 掌握了全局状态和局部状态的使用场景
- 学会了在组件间共享状态

### 5.2.3 数据库设计

**（1）数据库设计原则**
- 理解了数据库范式化和反范式化的平衡
- 掌握了主键、外键、索引的设计原则
- 学会了使用ENUM类型存储固定枚举值

**（2）数据库优化**
- 理解了索引对查询性能的影响
- 学会了分析慢查询并优化
- 掌握了数据库事务的隔离级别

### 5.2.4 系统集成

**（1）前后端分离架构**
- 理解了前后端分离的优势和实现方式
- 掌握了RESTful API的设计规范
- 学会了处理跨域（CORS）问题

**（2）微服务集成**
- 理解了服务间通信的方式（HTTP/REST）
- 掌握了服务健康检查和容错处理
- 学会了处理外部服务调用失败的情况

## 5.3 遇到的困难和解决方案

### 5.3.1 技术难题

**（1）问题：处方明细数据反序列化失败**

**现象**：前端提交处方创建请求时，后端接收到`details`字段为`null`，导致"处方明细不能为空"的错误。

**原因分析**：Jackson在反序列化嵌套的`List<PrescriptionDetailRequest>`时，由于`@Valid`注解和复杂嵌套结构的组合，导致反序列化失败。

**解决方案**：
- 将Controller方法的参数从`@RequestBody PrescriptionCreateRequest`改为`@RequestBody Map<String, Object>`
- 手动解析请求体中的JSON数据
- 手动构建`Prescription`和`PrescriptionDetail`对象
- 添加详细的日志记录，便于调试

**收获**：深入理解了Jackson的序列化/反序列化机制，学会了手动处理复杂数据结构。

**（2）问题：数据库ENUM类型与Java代码不匹配**

**现象**：审核结果和出库原因保存到数据库时出现"Data truncated for column"错误。

**原因分析**：数据库使用ENUM类型存储（如`audit_result ENUM('pass','warning','reject')`），但Java代码尝试保存中文字符串（如"通过"、"拒绝"）。

**解决方案**：
- 在Controller层进行值转换，将前端传入的中文值转换为ENUM值
- 在Entity类中使用`@JsonGetter`方法，将数据库中的ENUM值转换为中文返回给前端
- 统一规范：数据库存储英文ENUM值，前端展示中文值

**收获**：理解了数据库ENUM类型的特性，学会了处理中英文值转换的问题。

**（3）问题：库存扣减的数据一致性**

**现象**：并发发药时可能出现库存扣减不准确的情况。

**原因分析**：多个请求同时读取库存、扣减库存，没有使用数据库锁机制。

**解决方案**：
- 使用`@Transactional`注解保证事务的原子性
- 在Service层先检查库存，再扣减库存
- 使用数据库行锁（`SELECT ... FOR UPDATE`）防止并发问题
- 将库存检查和扣减放在同一个事务中

**收获**：深入理解了数据库并发控制和事务管理的重要性。

### 5.3.2 架构设计难题

**（1）问题：前后端数据格式不统一**

**现象**：前端期望的日期格式、枚举值格式与后端不一致。

**解决方案**：
- 使用`@JsonFormat`注解统一日期格式
- 使用`@JsonGetter`方法统一枚举值格式
- 建立统一的DTO规范，定义前后端交互的数据格式

**收获**：理解了前后端数据契约的重要性，学会了设计统一的数据传输格式。

**（2）问题：Python服务调用的错误处理**

**现象**：Python审核服务不可用时，导致整个处方审核流程阻塞。

**解决方案**：
- 实现服务健康检查机制（`isServiceAvailable()`）
- 在调用前检查服务可用性
- 服务不可用时提供友好的错误提示
- 设计降级处理机制，允许跳过自动审核，直接进入人工审核

**收获**：理解了外部服务集成中的容错和降级处理的重要性。

### 5.3.3 团队协作问题

**（1）问题：前后端接口对接困难**

**现象**：前端和后端开发进度不一致，接口对接时发现参数不匹配。

**解决方案**：
- 提前定义API接口规范（使用Swagger或Markdown文档）
- 使用Mock数据进行前端开发
- 定期进行接口联调
- 建立统一的错误码和响应格式规范

**收获**：理解了接口设计文档和团队协作规范的重要性。

## 5.4 心得体会

### 5.4.1 对软件工程的理解

通过本次项目实践，我们深刻体会到了软件工程的重要性：

**（1）需求分析的重要性**
- 在项目初期，我们花费了大量时间进行需求分析和系统设计
- 详细的需求分析帮助我们明确了功能边界和实现方式
- 避免了在开发过程中频繁变更需求，提高了开发效率

**（2）系统设计的重要性**
- 良好的系统设计是项目成功的基础
- 合理的模块划分和接口设计，使得开发过程更加顺畅
- 数据模型设计直接影响系统的性能和可维护性

**（3）文档的重要性**
- 完善的文档（设计文档、API文档、部署文档）是团队协作的基础
- 文档帮助我们理清思路，避免遗漏重要细节
- 详细的文档也有助于后期的维护和扩展

### 5.4.2 对技术学习的感悟

**（1）理论与实践结合**
- 课堂上学到的理论知识需要通过实践才能真正理解和掌握
- 在实际项目中遇到问题时，理论知识指导我们找到解决方案
- 实践过程中发现的新问题，促使我们深入学习相关技术

**（2）技术栈的选择**
- 技术栈的选择应该基于项目需求，而不是盲目追求新技术
- Spring Boot、Vue3等成熟框架大大提高了开发效率
- 合理的框架组合能够发挥各框架的优势，避免重复造轮子

**（3）持续学习的重要性**
- 技术在不断更新，需要保持持续学习的态度
- 遇到新技术时，要学会查阅官方文档和最佳实践
- 通过阅读优秀的开源项目代码，可以学习到很多设计模式和最佳实践

### 5.4.3 对团队协作的体会

**（1）明确的分工**
- 团队成员的明确分工有助于提高开发效率
- 每个人专注于自己擅长的领域，能够发挥最大价值
- 但也需要在关键节点进行交流和协调

**（2）有效的沟通**
- 定期的沟通会议帮助我们及时发现问题并协调解决
- 使用Git进行版本控制，避免了代码冲突
- 使用项目管理工具（如GitHub Issues）跟踪任务进度

**（3）相互学习**
- 团队成员之间的技术交流促进了共同进步
- 后端开发人员理解了前端的开发流程，前端开发人员理解了API设计原则
- 这种跨领域的理解有助于更好地协作

### 5.4.4 对项目管理的认识

**（1）时间管理**
- 合理的项目计划和时间分配是项目成功的关键
- 使用里程碑管理项目进度，及时调整计划
- 预留缓冲时间应对突发问题

**（2）版本控制**
- Git的使用帮助我们管理代码版本，避免了代码丢失
- 分支管理策略（如Git Flow）有助于团队协作
- 提交信息规范化，便于代码审查和问题追踪

**（3）测试的重要性**
- 单元测试和集成测试帮助我们尽早发现bug
- 测试用例的编写也促使我们思考边界情况
- 虽然本次项目测试覆盖率不高，但我们认识到了测试的重要性

## 5.5 不足之处和改进方向

### 5.5.1 技术层面的不足

**（1）测试覆盖率不足**
- 当前项目缺少完整的单元测试和集成测试
- 缺乏自动化测试流程
- **改进方向**：编写单元测试，使用JUnit和Mockito；建立CI/CD流程，实现自动化测试

**（2）性能优化不足**
- 部分查询没有使用索引优化
- 缺少缓存机制（如Redis）
- 大量数据查询时可能出现性能问题
- **改进方向**：优化数据库查询，添加索引；引入Redis缓存热点数据；使用分页查询优化大数据量场景

**（3）安全性加强**
- 密码强度验证不够严格
- 缺少请求频率限制（防止暴力破解）
- 缺少SQL注入防护（虽然使用了JPA，但仍需注意）
- **改进方向**：加强密码策略；实现请求限流；定期进行安全审计

### 5.5.2 功能层面的不足

**（1）功能不够完善**
- 缺少数据导入导出功能
- 缺少报表统计功能
- 缺少消息通知功能
- **改进方向**：实现Excel导入导出；开发数据统计报表；集成消息通知系统

**（2）用户体验待优化**
- 部分操作缺少加载提示
- 错误提示不够友好
- 缺少操作帮助文档
- **改进方向**：优化UI交互，添加加载动画；改进错误提示文案；编写用户操作手册

### 5.5.3 架构层面的不足

**（1）代码结构优化**
- 部分Service层代码过于复杂，职责不够单一
- 缺少统一的参数验证机制
- **改进方向**：重构代码，遵循单一职责原则；使用统一的参数验证工具

**（2）文档完善**
- API文档不够详细
- 缺少部署运维文档
- **改进方向**：使用Swagger自动生成API文档；编写详细的部署和运维文档

## 5.6 总结

本次软件设计实践项目是一次非常有价值的经历。通过完成"医院药房智能管理系统"的设计与开发，我们不仅掌握了Spring Boot、Vue3、MySQL等技术的实际应用，更重要的是培养了软件工程的思维方式，提高了分析问题、解决问题的能力。

在项目开发过程中，我们遇到了许多技术难题和架构挑战，但通过查阅文档、团队讨论、不断调试，最终都得到了解决。这些经历让我们深刻体会到了"纸上得来终觉浅，绝知此事要躬行"的道理。

同时，我们也认识到了项目的不足之处。测试覆盖率不足、性能优化不够、功能不够完善等问题，都是未来需要改进的方向。但正是这些不足，让我们更加清楚地认识到软件开发的复杂性和持续改进的重要性。

最后，感谢课程提供的实践机会，感谢团队成员的协作配合，感谢老师们的指导帮助。本次项目不仅是一次技术实践，更是一次成长和收获的旅程。我们将继续学习，不断提升自己的技术能力和工程素养，为未来的软件开发生涯打下坚实的基础。

---